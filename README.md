Министерство образование Республики Беларусь
Учреждение образования «Белорусский государственный университет информатики и радиоэлектроники»

Факультет инженерно-экономический
Кафедра экономической информатики
Дисциплина «Программирование сетевых приложений»
     
«К ЗАЩИТЕ ДОПУСТИТЬ»Руководитель курсового проекта
________________ Д.В.Мельников___.____.2026     
     
     
     
     
     
     
     
     
     
ПОЯСНИТЕЛЬНАЯ ЗАПИСКА
к курсовому проекту
на тему:
«ПРИЛОЖЕНИЕ ДЛЯ АВТОМАТИЗАЦИИ ЗАПИСИ К ВРАЧУ С МОДУЛЕМ НАПОМИНАНИЙ ПАЦИЕНТАМ»

БГУИР 6-05-0611-01 009 ПЗ
     
     
Выполнил студент группы 414301БЕЛЯКОВ Владислав Русланович
______________________________
       (подпись студента)
Курсовой проект представлен на проверку ___.____.2026______________________________
       (подпись студента)     
     
     
     
     
     
     
     
     
     
     
     
     
     
Минск 2026
    РЕФЕРАТ
    
     БГУИР КП 6-05-0611-01 09 ПЗ
     
     Беляков В. Р. Приложение для автоматизации записи к врачу с модулем напоминаний пациентам : пояснительная записка к курсовому проекту / В. Р. Беляков. – Минск : БГУИР, 2026. – 95 с.

     Пояснительная записка 95 с., 42 рис., 7 табл., 25 источников, 4 приложения
    
     JAVA, POSTGRESQL, JAVAFX, TCP/IP, СОКЕТЫ, КЛИЕНТ-СЕРВЕРНАЯ АРХИТЕКТУРА, МОДУЛЬ НАПОМИНАНИЙ, РОЛЕВАЯ МОДЕЛЬ ДОСТУПА.
     
     Цель проектирования: повысить эффективность работы медицинских учреждений, уменьшить количество пропущенных приёмов, сэкономить время персонала и сделать процесс записи более удобным для пациентов.
     Методология проведения работы: в процессе проектирования применены современные подходы к разработке программного обеспечения; объектно-ориентированное проектирование с использованием языка Java и фреймворка JavaFX для графического интерфейса; паттерны проектирования Command, DAO, Singleton, Builder, Observer и Strategy; хранение данных на стороне сервера в реляционной базе данных PostgreSQL; взаимодействие клиента и сервера через сериализованные объекты команд с аутентификацией на основе токенов сессий и хешированием паролей по алгоритму SHA-256; методология итеративной разработки с поэтапным добавлением функциональности для трёх ролей пользователей – пациент, врач, администратор.
     Результаты работы: проведен анализ существующих решений для медицинских учреждений, разработана клиент-серверная архитектура приложения с четким разделением ответственности, спроектирована реляционная база данных с таблицами, реализована система ролей с различными уровнями доступа, разработаны механизмы безопасности.
     Область применения: разработанное приложение может быть использовано в медицинских учреждениях различного профиля для автоматизации работы регистратуры и ведения расписания врачей, а также как основа для более сложных медицинских информационных систем.


СОДЕРЖАНИЕ
Введение	6
1 Анализ литературных источников и программных решений	8
 1.1 Описание и анализ предметной области	8
1.2 Обзор функциональности аналогов программного средства	10
2 Моделирование предметной области и разработка требований
 к программному средству	12
2.1 Анализ и формализация бизнес-процессов предметной области	12
2.2 Анализ требований к программному средству и разработка	18
      их спецификации	18
2.3 Образ предлагаемого решения	30
3 Проектирование и разработка программного средства	34
3.1 Архитектурные решения и технологии реализации
        программного средства	34
3.2 Проектирование и разработка пользовательского интерфейса	39
3.3 Разработка модели данных	42
3.4 Описание статических и динамических аспектов поведения
        программных объектов	50
3.5 Разработка и описание алгоритмов, реализующих бизнес-логику
        программного средства	54
3.6 Механизмы обеспечения информационной безопасности	59
4 Тестирование и проверка работоспособности
   программного средства	61
5 Руководство по установке (развертыванию) программного средства	64
5.1 Руководство по установке (развертыванию) программного средства	64
5.2 Руководство пользователя	66
Заключение	73
Список использованных источников	74
Приложение А (обязательное) Справка о проверке курсового проекта
                           на плагиат	76
Приложение Б (обязательное) Листинг программаного кода	77
Приложение В (обязательное) Листинг скрипта иницализации
                          базы данных	92



ВВЕДЕНИЕ
     
     В современном мире темп жизни постоянно ускоряется, и вопросы эффективного управления временем становятся критически важными как для отдельного человека, так и для организаций в целом. Сфера здравоохранения не является исключением. Одной из ключевых проблем, с которой сталкиваются пациенты и медицинский персонал, является организация процесса записи на прием. Традиционные методы часто приводят к образованию очередей, нерациональному использованию рабочего времени сотрудников и ошибкам, связанным с «человеческим фактором». Кроме того, высокая загруженность пациентов нередко становится причиной пропуска визитов к врачу из-за банальной забывчивости, что снижает эффективность лечения и нарушает график работы медицинского учреждения.
     Актуальность данной работы обусловлена необходимостью цифровизации и автоматизации рутинных процессов в медицинских учреждениях. Разработка приложения для автоматизации записи к врачу позволяет решить ряд проблем: минимизировать время ожидания, исключить двойную запись, оптимизировать нагрузку на персонал и повысить удовлетворенность пациентов. Внедрение отдельного модуля напоминаний призвано снизить процент неявки пациентов, что напрямую влияет на доступность медицинской помощи и экономическую эффективность работы клиники.
     Объектом исследования в данной курсовой работе являются процессы взаимодействия пациента и медицинского учреждения. Предметом исследования выступают методы автоматизации записи и оповещения пациентов с использованием десктопного приложения.
     Целью курсовой работы является повышение эффективности работы медицинских учреждений, уменьшение количества пропущенных приёмов, экономия времени персонала и повышение удобства процесса записи для пациентов.
     Для достижения поставленной цели в рамках курсового проектирования решаются следующие задачи:
     1 Провести анализ предметной области автоматизации записи к врачу, изучить существующие программные аналоги и систематизировать функциональные требования к разрабатываемому приложению.
     2 Спроектировать реляционную модель базы данных, нормализованную до третьей нормальной формы, обеспечивающую хранение информации о пользователях, врачах, пациентах, расписании, записях на приём, медицинских протоколах, напоминаниях и журнале аудита с соблюдением принципов ссылочной целостности.
     3 Разработать многопоточное серверное приложение на языке Java с использованием TCP/IP-сокетов для приёма и параллельной обработки клиентских запросов, поступающих в виде сериализованных объектов-команд.
     4 Реализовать систему аутентификации на основе токенов сессий с применением алгоритма хеширования паролей SHA-256, а также механизм ограничения срока действия сессий и автоматической инвалидации токенов при выходе из системы.
     5 Разработать трёхуровневую ролевую модель доступа (PATIENT, DOCTOR, ADMIN), обеспечивающую разграничение прав пользователей как на уровне доступных команд, так и на уровне персонализации интерфейса и данных.
     6 Реализовать модуль записи пациентов на приём, включающий динамическую загрузку списка врачей, отображение свободных временных слотов с учётом занятости, бронирование выбранного слота с автоматической генерацией напоминаний и освобождение слота при отмене записи.
     7 Разработать модуль ведения электронной медицинской карты, позволяющий врачу заполнять протокол приёма, сохранять его в базе данных и предоставлять пациенту доступ к истории визитов с возможностью просмотра диагнозов и назначений.
     8 Реализовать подсистему напоминаний с фоновым планировщиком на стороне сервера, обеспечивающую автоматическую отправку уведомлений о предстоящих приёмах за заданные интервалы времени и отображение актуального статуса напоминаний в интерфейсе пациента.
     9 Разработать административный модуль, включающий управление учётными записями пользователей, справочником врачей, просмотр всех записей и системных журналов аудита с фильтрацией и поиском, а также формирование аналитических отчётов с экспортом в формат CSV.
     10 Провести тестирование ключевых алгоритмов взаимодействия клиента и сервера, обработки граничных случаев и отказоустойчивости, а также подготовить руководство пользователя, руководство по установке и развёртыванию системы.
     Практическая значимость работы заключается в создании готового к использованию программного продукта, который может быть внедрен в работу небольших частных клиник или государственных учреждений для оптимизации их деятельности.
     Курсовой проект выполнен самостоятельно, проверен в системе «Атиплагиат». Процент оригинальности составляет 94%. Цитирования обозначены ссылками на публикации, указанными в «Списке использованных источников». Скриншот приведен в приложении Б.
     
1 АНАЛИЗ ЛИТЕРАТУРНЫХ ИСТОЧНИКОВ И  
       ПРОГРАММНЫХ РЕШЕНИЙ
    
        1.1 Описание и анализ предметной области
     
     Система автоматизации записи к врачу с модулем напоминаний представляет собой специализированное программное решение, предназначенное для оптимизации взаимодействия между медицинским учреждением и пациентами. В отличие от традиционных бумажных журналов или отдельных электронных календарей, такая система изначально проектируется как целостная среда управления приёмами, объединяющая расписание врачей, механизм бронирования слотов и автоматическое информирование пациентов. Это означает, что каждая операция записи рассматривается не только как техническая фиксация времени, но и как элемент комплексного сервиса, включающего контроль занятости, обработку отмен и превентивное оповещение.
     Предметная область охватывает несколько взаимосвязанных функциональных блоков, которые в совокупности обеспечивают полный жизненный цикл записи на приём.
     Управление субъектами системы включает регистрацию пользователей (пациентов, врачей, администраторов), их идентификацию и аутентификацию, назначение ролевых полномочий, а также управление жизненным циклом учётных записей (активация, блокировка, изменение данных). На этом уровне решаются задачи создания профилей пациентов с указанием контактной информации, привязки врачей к их специализациям и кабинетам, а также разграничения доступа к функциям системы в соответствии с ролями.
     Управление расписанием обеспечивает формирование и ведение графика работы медицинских специалистов. В него входят: создание шаблонов расписания (например, «понедельник–пятница с 9:00 до 15:00»), автоматическая генерация временных слотов заданной длительности (типовой слот – 20 минут), ручная корректировка (блокировка слотов на время обеда, отпуска врача), а также отображение свободных и занятых интервалов. Каждый слот характеризуется датой, временем начала и окончания, а также флагами занят и заблокирован.
     Обработка записи на приём является ключевым с точки зрения бизнес-логики. Он реализует: поиск свободных слотов по выбранному врачу и дате, валидацию выбранного времени, создание записи со статусом PENDING (ожидает подтверждения) или CONFIRMED, автоматическое резервирование слота, а также обработку отмены или переноса записи с освобождением слота. При создании записи фиксируются дата и время визита, причина обращения, идентификаторы пациента и врача.
     Модуль напоминаний обеспечивает автоматическое уведомление пациентов о предстоящих приёмах. Для каждой созданной записи система генерирует одно или несколько напоминаний с заданными интервалами (например, за 24 часа и за 2 часа до визита). Канал связи (email, СМС) и статус отправки фиксируются в базе данных. Фоновый планировщик на сервере периодически сканирует таблицу напоминаний и, когда наступает время отправки, инициирует доставку, после чего статус меняется на SENT. Пациент может просматривать свои напоминания, изменять интервалы и каналы, а также отключать ненужные.
     Медицинская карта и отчётность агрегирует данные о завершённых и пропущенных приёмах. Врач после визита заполняет протокол: жалобы, осмотр, диагноз, назначения и рекомендации. Эта информация становится доступной пациенту в разделе «История визитов». Администратор может формировать отчёты по нагрузке врачей, явке пациентов, эффективности напоминаний и экспортировать их в формате CSV.
     Таким образом, предметная область автоматизации записи к врачу представляет собой сложную систему взаимосвязанных бизнеспроцессов и сущностей, включающую участников с различными ролями, врачебные расписания, временные слоты, записи на приём, медицинские протоколы, напоминания и аналитические отчёты. Автоматизация этой области требует специализированного программного обеспечения, сочетающего развитую логику управления расписанием, гибкую настройку напоминаний, надёжные механизмы разграничения прав доступа и мощные инструменты аналитики.
     
     1.2 Обзор функциональности аналогов программного средства
     
     Для определения оптимальных архитектурных и функциональных решений проведён сравнительный анализ трёх характерных представителей рынка систем автоматизации записи к врачу, а также одного универсального календарного сервиса, используемого в медицинской практике: специализированная медицинская информационная система «1С:Медицина», облачный сервис управления записями «YCLIENTS», комплексная платформа для клиник «MedExpert».
     «1С:Медицина» представляет собой отраслевое решение на базе технологической платформы 1С, предназначенное для автоматизации медицинских учреждений. Система обеспечивает ведение электронных медицинских карт, учёт услуг, формирование расписания врачей и интеграцию с бухгалтерским контуром предприятия. К безусловным достоинствам относятся соответствие требованиям законодательства (в том числе в части регламентированной отчётности), возможность локального развёртывания без постоянного доступа к интернету и глубокая интеграция с другими продуктами 1С. Вместе с тем модуль записи на приём в «1С:Медицина» ориентирован на работу регистратора: пациент не имеет возможности самостоятельной онлайнзаписи, а модуль напоминаний либо отсутствует, либо реализован в виде ручного обзвона. Стоимость лицензии варьируется от 200 000 до 600 000 рублей в зависимости от числа рабочих мест, дополнительно оплачивается внедрение и сопровождение. Система разворачивается на СУБД Microsoft SQL Server или PostgreSQL; для работы требуется выделенный сервер и лицензированная операционная система Windows.
     YCLIENTS является облачным сервисом для управления записью клиентов в сфере услуг. Система предоставляет пациенту возможность самостоятельной онлайнзаписи через вебинтерфейс или мобильное приложение, поддерживает автоматическую отправку SMS и emailнапоминаний, а также интеграцию с социальными сетями. Достоинствами YCLIENTS являются простота внедрения, удобный интерфейс и гибкая настройка расписания. Однако сервис является исключительно облачным, что создаёт зависимость от качества интернетсоединения и не позволяет локализовать данные на серверах медицинского учреждения (что может противоречить требованиям к защите персональных данных). Кроме того, YCLIENTS не предоставляет встроенных средств ведения электронной медицинской карты – система фокусируется исключительно на записи и учёте клиентов. Модуль напоминаний работает в режиме «fireandforget» без возможности гибкой настройки интервалов и каналов для каждого пациента. Стоимость подписки – от 1 500 до 6 000 рублей в месяц в зависимости от тарифа.
     MedExpert – комплексная система для управления частными медицинскими центрами, включающая модули расписания, электронной карты, лабораторной информационной системы и финансового учёта. Система поддерживает автоматическую рассылку напоминаний, интеграцию с сайтом клиники для онлайнзаписи и формирование аналитических отчётов. MedExpert может разворачиваться как локально, так и в облаке. К недостаткам можно отнести высокую стоимость, сложность настройки и перегруженный интерфейс. Модуль напоминаний в базовой версии отсутствует либо требует дополнительной оплаты. Пользователи отмечают недостаточную гибкость в управлении временными слотами и отсутствие функции автоматической блокировки слотов при превышении лимита записей.

Таблица 1.1 – Сравнение функциональности аналогов программного средства
Критерий1С: МедицинаYCLIENTSMedExpertПлатформаДесктоп Веб / МобильнаяДесктопСпециализа-цияМедицинскаяУниверсальная (услуги)МедицинскаяУправление расписаниемДаДаДаЗапись пациентовДа (с картотекой)Да (с CRM)ДаМодуль напоминанийSMS, EmailSMS, Email, PushНет / ручной обзвонАвтономностьЧастично (требуется лок. сеть)Нет (нужен интернет)ДаСложность внедренияВысокаяНизкаяСредняяСтоимостьВысокаяЕжемесячная платаНизкая / СредняяОтчетностьРазвитаяБазоваяБазовая
     Таким образом, существует потребность в легковесном, автономном десктопном приложении, которое сочеталло бы простоту использования с ключевыми функциями: ведение картотеки врачей и пациентов, удобное расписание и, обязательно, встроенный модуль напоминаний (например, отображение списка пациентов для обзвона или автоматическая отправка email). Разработка такого приложения на платформе Java в среде IntelliJ IDEA является актуальной и позволит закрыть выявленные недостатки аналогов, предоставив медицинским учреждениям доступный и эффективный инструмент автоматизации.
     
    

   2 МОДЕЛИРОВАНИЕ ПРЕДМЕТНОЙ ОБЛАСТИ
      И РАЗРАБОТКА ТРЕБОВАНИЙ
      К ПРОГРАММНОМУ СРЕДСТВУ
     
     2.1 Анализ и формализация бизнес-процессов предметной области
     
     Business Process Model and Notation (нотация моделирования бизнес-процессов) – это система условных обозначений, которая отображает бизнес-процессы с помощью блок-схем. BPMN диаграмма показывает в какой последовательности совершаются рабочие действия и перемещаются потоки информации. 
     Четкое представление бизнес-процессов – основа успеха любого проекта и организации. Диаграммы BPMN стали универсальным языком для моделирования процессов, понятным как аналитикам, так и разработчикам. Но между созданием просто "красивой схемы" и построением действительно рабочей модели процесса лежит пропасть. Профессиональные BPMN-диаграммы позволяют не только визуализировать, но и трансформировать бизнес, обнаруживая узкие места и возможности для оптимизации.
     На рисунке 2.1 представлена схема записи пациента к врачу.
     
Рисунок 2.1 – BPMN-диаграмма процесса «Запись к врачу»
     
     Для формализации бизнес-процессов предметной области используется методология IDEF0, которая представляет систему в виде иерархии функциональных блоков[6]. Каждый блок описывает функцию с четырьмя типами связей: входные данные (слева), управляющие воздействия (сверху), механизмы исполнения (снизу) и выходные данные (справа).
     На рисунке 2.1 представлена контекстная диаграмма верхнего уровня (A-0), описывающая систему автоматизации записи к врачу в целом. Центральным элементом является блок «Управлять записью к врачу с модулем напоминаний».

Рисунок 2.1 – Контекстная диаграмма верхнего уровня (А-0)

     Входными данными для системы являются: данные пациента, запросы на запись и информация о свободных слотах расписания. Управляющими воздействиями выступают нормативные требования к здравоохранению и политика конфиденциальности медицинских данных. Механизмами исполнения являются пациент, врач, регистратор и программная система. На выходе формируются: подтверждённая запись, напоминание пациенту и отчёт о записях.
     
     Декомпозиция контекстной диаграммы представлена на рисунке 2.2. Процесс управления записью к врачу разбивается на восемь ключевых подпроцессов.
     
     
Рисунок 2.2 – Декомпозиция контекстной диаграммы (A1)

     1 Процесс «Обработать запрос на запись» обеспечивает валидацию запроса для дальнейшего использования.
     2 Процесс «Подобрать доступный слот» реализует отображение пациенту или регистратору актуальной информации о свободных временных слотах. Входными данными служат обновлённое расписание из A5 и выбранный врач с специализацией из A1.
     3 Процесс «Подтвердить запись» является центральным бизнес-процессом системы. На его вход поступают выбранные пациентом слоты из A2. Результатом является информация о записи, которая передаётся в процесс A4.
     4 Процесс «Сформировать отчёт» выполняет верификацию записи со стороны врача или регистратора. Подтверждённая запись изменяет статус в базе данных и инициирует процесс отправки уведомления пациенту.
     На рисунке 2.3 представлена декомпозиция процесса «Обработать запрос на запись»
     
     
Рисунок 2.3 – Декомпозиция процесса «Обработать запрос на запись»
     
     Основне этапы процесса:
     1 Идентифицировать пациента.
     2 Валидировать параметры записи.
     3 Сформировать валидированный запрос.
     На рисунке 2.4 представлена декомпозиция процесса «Идентифицировать пациента»
     
     
     
     
Рисунок 2.4 – Декомпозиция процесса «Идентифицировать пациента»
     
     Основные эиапы процесса:
     1 Ввести идентификаторы.
     2 Выполнить автоматический поиск в регистре.
     3 Обработать результат поиска.
     4 Выполнить верификацию.
     Таким образом, из ранее описанных IDEF0диаграмм процесса «Управлять записью к врачу» и их декомпозиции можно сделать вывод, что эффективная система записи к врачу основывается на чётко организованных этапах, включая сбор идентификационных данных пациента, автоматическую и ручную верификацию, валидацию параметров записи, подбор свободного слота, подтверждение и отправку напоминания. При этом взаимодействие пациентов, регистраторов и врачей через приложение, а также учёт нормативных требований и политики конфиденциальности медицинских данных играют ключевую роль.
     Грамотная интеграция всех компонентов позволяет оптимизировать процесс записи, минимизировать ошибки и обеспечивать точное выполнение задач – от первичного запроса до подтверждённого визита и последующего напоминания.
     Это особенно важно для предотвращения перебоев в оказании медицинской помощи и для повышения общей эффективности операционной деятельности клиники. Продуманное управление записью к врачу способствует не только рациональному использованию рабочего времени медицинского персонала, но и долгосрочной стабильности бизнеса медицинской организации за счёт роста удовлетворённости пациентов и снижения административной нагрузки.
     
     2.2 Анализ требований к программному средству и разработка
           их спецификации
     
     Разрабатываемое приложение для автоматизации записи к врачу должно обеспечивать управление расписанием медицинских специалистов, бронирование временных слотов пациентами и автоматическую рассылку напоминаний. Бизнес-логика системы реализуется исключительно на серверной стороне. Клиентская часть обеспечивает доступ ко всем функциям через удобный графический интерфейс с разграничением прав пользователей. Система должна поддерживать не менее 15 высокоуровневых вариантов использования.
     В системе предусмотрены три роли пользователей: администратор, управляющий учётными записями и системными настройками; врач, управляющий собственным расписанием и просматривающий список пациентов; пациент, самостоятельно записывающийся к врачу и получающий напоминания.
     Для более детального разбора функциональных возможностей каждой роли пользователей на рисунке 2.5 приведена диаграмма вариантов использования (Use Case Diagram) в нотации UML[7].
     
     
     
     
     
     
     
     
     
     
     
     
     
Рисунок 2.5 – Диаграмма вариантов использования
     
     Описание действующих лиц – пользователей программного средства приведено в таблице 2.1.
     
Таблица 2.1 – Описание действующих лиц
Класс пользователйОписаниеВарианты использованияПользовательВсе авторизованные пользователи системыUC-1 Вход UC-2 Изменение личных данных UC-3 Просмотр расписанияПациентФизическое лицо, записывающееся на приёмUC-4 Регистрация UC-5 Запись к врачу UC-6 Просмотр своих записей UC-7 Отмена/перенос записи UC-8 Просмотр истории визитовВрачМедицинский специалист, принимающий пациентовUC-9 Управление расписанием UC-10 Подтверждение записи UC-11 Просмотр списка пациентов UC-12 Внесение отметки о явкеАдминистраторУправляет пользователями и системойUC-13 Управление всеми записями UC-14 Ручная запись пациента UC-15 Управление напоминаниями UC-16 Формирование отчётов
UC-17 Управление учётными записями UC-18 Управление справочником врачей UC-19 Просмотр системных журналов     
     Подробное описание диаграммы вариантов использования представлено ниже в таблице 2.2.
     
Таблица 2.2 – Описание вариантов использования
12Имя варианта использованияUC-1 ВходКраткое описаниеАвторизация пользователя по логину и паролю для доступа к системе.Действующие лицаВсе пользователи системыПредусловияПользователь зарегистрирован в системе.Основной поток1. Пользователь вводит логин и пароль. 2. Нажимает «Войти». 3. Система проверяет учётные данные. 4. При успешной проверке Продолжение таблицы 2.2
12открывается главный экран соответствующей роли.Альтернативный потокПри неверных данных отображается ошибка, пользователь повторяет ввод.ИсключенияНеверный логин или пароль; учётная запись заблокирована.ПостусловияПользователь авторизован и перенаправлен на главный экран.Имя варианта использованияUC-2 Изменение личных данныхКраткое описаниеИзменение данных о пользователе, хранящихся в системе.Действующие лицаВсе пользователи системыПредусловияЛичные данные пользователя есть в системе.Основной поток1. Пользователь вводит логин и пароль. 2. Нажимает «Войти». 3. Система проверяет учётные данные. 4. При успешной проверке открывается главный экран соответствующей роли. Альтернативный потокПри неверных данных отображается ошибка, пользователь повторяет ввод.ИсключенияНеверный логин или пароль; учётная запись заблокирована.ПостусловияПользователь авторизован и перенаправлен на главный экран.Имя варианта использованияUC3 Просмотр расписанияКраткое описаниеПользователь просматривает свободные слоты для записи к врачу.Действующие лицаПациент, врач (для своего расписания), администраторПредусловияПользователь авторизован.Основной поток1. Пользователь выбирает раздел «Расписание». 2. Фильтрует по специальности, врачу, дате. 3. Система отображает доступные временные слоты. 4. Пользователь может выбрать слот для записи (переход к UC5).Продолжение таблицы 2.2
12Альтернативный потокВрач видит только своё расписание с возможностью редактирования (UC9).ИсключенияНет доступных слотов на выбранную дату – отображается сообщение.ПостусловияПользователь получил актуальную информацию о свободном времени.Имя варианта использованияUC4 РегистрацияКраткое описаниеСоздание новой учётной записи пациента.Действующие лицаПациент (новый пользователь)ПредусловияПользователь не авторизован.Основной поток1. Пользователь нажимает «Регистрация» на экране входа. 2. Заполняет обязательные поля: ФИО, дата рождения, номер полиса, телефон, email. 3. Придумывает логин и пароль. 4. Подтверждает согласие на обработку ПДн. 5. Система проверяет уникальность логина и существование пациента в МИС. 6. Отправляет SMS/email с кодом подтверждения. 7. Пользователь вводит код, учётная запись активируется.Альтернативный потокЕсли пациент уже есть в МИС, но не имеет учётной записи – запись создаётся и связывается с существующей карточкой.Исключения– Логин занят.
– Пациент не найден в МИС (требуется обращение в регистратуру).
– Неверный код подтверждения.ПостусловияСоздана новая учётная запись пациента, пользователь авторизован.Имя варианта использованияUC5 Запись к врачуКраткое описаниеПациент выбирает доступного врача и временной слот для визита.Действующие лицаПациентПредусловияПациент авторизован, расписание просмотрено (UC3).
     
     
Продолжение таблицы 2.2
12Основной поток1. Пациент выбирает свободный слот из отображённого расписания. 2. Указывает причину записи (предварительный диагноз, жалобы). 3. Нажимает «Записаться». 4. Система проверяет, что слот ещё свободен. 5. Резервирует слот и создаёт запись. 6. Отправляет пациенту подтверждение на email/SMS.Альтернативный потокЕсли слот занят другим пользователем в момент подтверждения – отображается ошибка, пациент выбирает другой слот.Исключения– Выбран нерабочий день/время.
– Достигнут лимит записей для пациента в день.
– Отсутствует согласие на уведомления.ПостусловияСоздана запись на приём со статусом «Подтверждена» (или «Ожидает подтверждения»).Имя варианта использованияUC6 Просмотр своих записейКраткое описаниеПациент получает список своих предстоящих и прошлых записей.Действующие лицаПациентПредусловияПациент авторизован.Основной поток1. Пациент переходит в раздел «Мои записи». 2. Система отображает список: дата, время, врач, статус. 3. Пациент может выбрать запись для отмены (UC7) или просмотра деталей.Альтернативный потокВозможна фильтрация по статусу (предстоящие, прошедшие, отменённые).ИсключенияНет записей – отображается соответствующее сообщение.ПостусловияПациент ознакомлен со своими записями.Имя варианта использованияUC7 Отмена/перенос записиКраткое описаниеПациент отменяет или переносит существующую запись на другой слот.     
     
Продолжение таблицы 2.2
Действующие лицаПациентПредусловияПациент авторизован, есть предстоящие записи (UC6).Основной поток (отмена)1. Пациент выбирает запись из списка. 2. Нажимает «Отменить». 3. Подтверждает действие. 4. Система освобождает слот, изменяет статус записи на «Отменена пациентом». 5. Приходит уведомление об отмене.Альтернативный потокЕсли перенос выполняется менее чем за N часов до приёма (задано правилами), требуется согласование с администратором.Исключения– Слот для переноса уже занят.
– Время отмены/переноса нарушает нормативные сроки (например, менее чем за 2 часа).ПостусловияЗапись отменена или изменена, пациент уведомлён.Имя варианта использованияUC8 Просмотр истории визитовКраткое описаниеПациент просматривает архив визитов (завершённые приёмы) с возможностью увидеть диагнозы, назначения.Действующие лицаПациентПредусловияПациент авторизован.Основной поток1. Пациент переходит в «История визитов». 2. Система отображает список завершённых записей. 3. Пациент выбирает конкретный визит – видит дату, врача, диагноз, рекомендации (если внесены врачом).Альтернативный потокДоступна фильтрация по периоду (неделя, месяц, год).ИсключенияНет завершённых визитов – сообщение «История пуста».ПостусловияПациент ознакомлен с историей визитов.     
     
     
Продолжение таблицы 2.2
Имя варианта использованияUC9 Управление расписаниемКраткое описаниеВрач настраивает своё рабочее расписание: добавляет/блокирует слоты, задаёт перерывы, отпуска.Действующие лицаВрачПредусловияВрач авторизован.Основной поток1. Врач открывает календарь расписания. 2. Выбирает день. 3. Добавляет рабочие интервалы или блокирует занятое время (конференция, больничный). 4. Сохраняет изменения. 5. Система обновляет доступные слоты для пациентов.Альтернативный потокВрач может скопировать расписание с другого дня или недели.Исключения– Попытка удалить слот, на который уже есть записи (требуется подтверждение с уведомлением пациентов).
– Нарушение нормативов (обеденный перерыв короче минимума).ПостусловияРасписание врача обновлено, новые слоты видны пациентам.Имя варианта использованияUC10 Подтверждение записиКраткое описаниеВрач подтверждает запись пациента (отмечает, что пациент явился или подтверждает предварительную запись).Действующие лицаВрачПредусловияВрач авторизован, есть записи на текущий день.Основной поток1. Врач открывает список записей на день (UC11). 2. Выбирает запись пациента. 3. Нажимает «Подтвердить явку». 4. Система изменяет статус записи на «Выполнена».Альтернативный потокЕсли пациент не явился – врач отмечает «Неявка», запись получает соответствующий статус.Исключения– Запись уже обработана ранее.
– Врач пытается подтвердить запись на прошлый день (только через администратора).Продолжение таблицы 2.2
12ПостусловияСтатус записи обновлён, отражается в отчётности и истории пациента.Имя варианта использованияUC11 Просмотр списка пациентовКраткое описаниеВрач видит список пациентов, записанных к нему на выбранный день, с контактной информацией.Действующие лицаВрачПредусловияВрач авторизован.Основной поток1. Врач открывает «Список пациентов». 2. Выбирает дату (сегодня по умолчанию). 3. Система отображает таблицу: ФИО, время записи, причина, статус. 4. Врач может нажать на запись для просмотра деталей или подтверждения (UC10).Альтернативный потокВозможен поиск по ФИО пациенту.ИсключенияНет записей на выбранную дату – пустой список.ПостусловияВрач получил актуальную информацию о пациентах на приём.Имя варианта использованияUC12 Внесение отметки о явкеКраткое описаниеВрач или регистратор фиксирует, пришёл ли пациент на приём. (По сути, это часть UC10, но выделено отдельно.)Действующие лицаВрач, администраторПредусловияСуществует запись на текущую дату.Основной поток1. Пользователь открывает список записей. 2. Выбирает пациента. 3. Нажимает «Явился» или «Не явился». 4. Система обновляет статус записи, при необходимости отправляет уведомление (например, при неявке – в отчётность).Альтернативный потокМожно внести отметку задним числом (только администратор).ИсключенияЗапись уже имеет статус «Выполнена» или «Отменена».

Продолжение таблицы 2.2
12ПостусловияСтатус явки зафиксирован, используется для расчёта нагрузки и формирования отчётов.Имя варианта использованияUC13 Управление всеми записямиКраткое описаниеАдминистратор просматривает, редактирует, отменяет любые записи (всех пациентов, всех врачей).Действующие лицаАдминистраторПредусловияАдминистратор авторизован.Основной поток1. Администратор открывает раздел «Все записи». 2. Фильтрует по врачу, дате, статусу. 3. Выбирает запись, выполняет действие: отмена, перенос, изменение причины. 4. Система применяет изменения, уведомляет пациента (при отмене/переносе).Альтернативный потокМассовое действие (отмена записей за день из-за аварии).Исключения– Попытка отменить запись, которая уже выполнена.
– Нет прав для редактирования (роль «администратор» должна быть).ПостусловияЗаписи изменены в соответствии с действиями администратора.Имя варианта использованияUC14 Ручная запись пациентаКраткое описаниеАдминистратор создаёт запись от лица пациента (например, по телефону или для льготной категории).Действующие лицаАдминистраторПредусловияАдминистратор авторизован, пациент существует в базе.Основной поток1. Администратор выбирает «Запись для пациента». 2. Ищет пациента по ФИО/полису. 3. Открывает расписание врача (UC3). 4. Выбирает слот, указывает причину. 5. Сохраняет запись. 6. Система отправляет пациенту уведомление о созданной записи.Продолжение таблицы 2.2
12Альтернативный потокЕсли пациента нет в системе – администратор может зарегистрировать его (UC4 из интерфейса).Исключения– Выбранный слот уже занят (администратор видит реальное время).
– Пациент не дал согласия на обработку ПДн.ПостусловияСоздана запись от имени пациента, пациент уведомлён.Имя варианта использованияUC15 Управление напоминаниямиКраткое описаниеАдминистратор настраивает параметры автоматических напоминаний (время отправки, каналы, шаблоны).Действующие лицаАдминистраторПредусловияАдминистратор авторизован, модуль напоминаний включён в системе.Основной поток1. Администратор открывает «Настройки напоминаний». 2. Указывает период (за сколько часов/дней отправлять). 3. Выбирает каналы (SMS, email, push). 4. Редактирует текст шаблона. 5. Сохраняет настройки. 6. Система применяет изменения для всех будущих записей.Альтернативный потокМожно вручную запустить отправку напоминаний для выбранной группы записей.Исключения– Недостаточно средств на счету для SMS (интеграция с провайдером).
– Шаблон содержит недопустимые символы.ПостусловияНастройки напоминаний сохранены, автоматические отправки будут выполняться по новым правилам.     
     
     
     
     
Продолжение таблицы 2.2
Имя варианта использованияUC16 Формирование отчётовКраткое описаниеАдминистратор создаёт отчёты по записям, явке, нагрузке врачей и другим метрикам.Действующие лицаАдминистраторПредусловияАдминистратор авторизован, в системе накоплены данные за период.Основной поток1. Администратор переходит в «Отчёты». 2. Выбирает тип отчёта (по записям, по врачам, по неявкам). 3. Указывает период (день, неделя, месяц, произвольный). 4. Нажимает «Сформировать». 5. Система генерирует отчёт в формате таблицы/графика. 6. Администратор может экспортировать в PDF, Excel.Альтернативный потокАвтоматическая еженедельная отправка отчёта на email администратора (настраивается).Исключения– За выбранный период нет данных.
– Некорректный формат даты.ПостусловияОтчёт сформирован и доступен для скачивания или просмотра.Имя варианта использованияUC17 Управление учётными записямиКраткое описаниеАдминистратор создаёт, блокирует, удаляет учётные записи врачей и других администраторов.Действующие лицаАдминистраторПредусловияАдминистратор авторизован, есть права на управление пользователями.Основной поток1. Администратор открывает «Пользователи». 2. Выбирает действие: «Создать», «Редактировать», «Заблокировать». 3. Заполняет необходимые поля (логин, роль, ФИО, контакты). 4. Сохраняет. 5. Система отправляет новому пользователю приглашение/инструкции.Альтернативный потокПри блокировке – указать причину и срок блокировки.
Продолжение таблицы 2.2
12Исключения– Попытка создать пользователя с уже существующим логином.
– Блокировка единственного администратора (должно быть предупреждение).ПостусловияУчётная запись создана/изменена/заблокирована, пользователь уведомлён.Имя варианта использованияUC18 Управление справочником врачейКраткое описаниеАдминистратор ведёт список врачей (специальность, кабинет, часы приёма, допустимые причины).Действующие лицаАдминистраторПредусловияАдминистратор авторизован.Основной поток1. Администратор открывает «Справочник врачей». 2. Добавляет нового врача (ФИО, специальность, кабинет). 3. Привязывает к нему разрешённые причины записи и шаблон расписания. 4. Сохраняет. 5. Система обновляет доступные враги для записи пациентов.Альтернативный потокРедактирование существующего врача (смена кабинета, удаление). При удалении проверяется наличие будущих записей – требуется переназначение.Исключения– Специальность не найдена в справочнике специальностей.
– Попытка удалить врача, у которого есть активные записи (блокируется).ПостусловияСправочник врачей актуален, отражается в расписании и записи пациентов.Имя варианта использованияUC19 Просмотр системных журналовКраткое описаниеАдминистратор просматривает логи действий пользователей, ошибки, изменения данных.Действующие лицаАдминистратор     
Продолжение таблицы 2.2
ПредусловияАдминистратор авторизован, журналирование включено.Основной поток1. Администратор открывает «Системные журналы». 2. Выбирает тип событий (входы, изменения записей, ошибки). 3. Указывает период. 4. Нажимает «Поиск». 5. Система отображает список записей с временем, пользователем, действием. 6. Администратор может экспортировать журнал.Альтернативный потокФильтрация по конкретному пользователю или IP-адресу.Исключения– Журнал за указанный период не найден.
– Недостаточно прав (только у главного администратора).ПостусловияАдминистратор получил информацию о событиях в системе для анализа или аудита.     
     Такой структурированный подход позволяет выявить все возможные варианты использования системы на ранних этапах проектирования, что снижает риски появления ошибок и неучтённых требований в дальнейшем. Диаграмма вариантов использования описывает систему с точки зрения конечного пользователя, фокусируясь на том, какие цели он хочет достичь, а не на технических деталях реализации.

     2.3 Образ предлагаемого решения
     
     Разрабатываемое программное средство представляет собой десктопное клиент-серверное приложение для автоматизации записи пациентов на приём к врачам с модулем напоминаний. Система построена на основе многоуровневой архитектуры, в которой серверная часть реализует основную бизнес-логику, проверку прав доступа, взаимодействие с базой данных и фоновую отправку уведомлений, а клиентская часть предоставляет графический интерфейс пользователя, реализованный на платформе JavaFX. Такое разделение обеспечивает независимую разработку и модификацию каждого уровня, а также централизованное хранение и защиту данных.
     Взаимодействие между клиентом и сервером осуществляется по сети с использованием TCP/IP-сокетов. В качестве прикладного протокола обмена данными применяется передача сериализованных объектов команд (класс Command). Каждый запрос клиента представляет собой экземпляр Command, содержащий тип операции (перечисление CommandType), параметры в виде ассоциативного массива строк и токен сессии для аутентификации запросов (поле sessionToken), отсутствующее только у публичных команд LOGIN, REGISTER и PING. Ответ сервера также формируется в виде объекта Command, в котором поле статуса (CommandStatus) принимает значения OK, CLIENT_ERROR или SERVER_ERROR, а дополнительная информация возвращается в параметрах и поле message. Передача данных реализована посредством сериализации Java-объектов [9].
     Серверное приложение функционирует как долгоживущий процесс, прослушивающий входящие TCP-соединения на заранее сконфигурированном порту 5555[10]. Каждое новое подключение передаётся на обработку отдельному потоку (ClientThread), что позволяет параллельно обслуживать запросы нескольких клиентов без взаимных блокировок. Для взаимодействия с базой данных используется выделенный синглтон-объект DBhelper, обеспечивающий пул соединений с PostgreSQL и централизованное выполнение SQL-запросов. Безопасность пользовательских паролей гарантируется хранением их SHA-256-хешей и использованием уникальных токенов сессий, срок действия которых ограничен.
     Представленная функциональная архитектура демонстрирует модульный подход к построению системы с радиальной топологией информационных потоков. Центральным связующим звеном выступает ядро – «Система управления записью на приём с модулем напоминаний». Оно выполняет роль диспетчера, маршрутизируя запросы от клиентов к соответствующим обработчикам, обеспечивая многоуровневую валидацию операций и поддерживая целостность бизнес-логики.
     Вокруг ядра сгруппированы специализированные функциональные модули, осуществляющие обмен данными с центром:
     Аутентификация и контроль доступа. Модуль отвечает за первичную безопасность системы. Он передаёт в ядро введённые пользователем логин и пароль. После успешной верификации ядро создаёт сессию и возвращает клиенту токен, идентификатор пользователя и его роль (PATIENT, DOCTOR, ADMIN). Все последующие запросы снабжаются этим токеном, что позволяет серверу однозначно идентифицировать пользователя и проверять его права.
     Управление пользователями и ролями. Инструмент администрирования персоналом. Модуль направляет в систему команды на получение списка всех пользователей, блокировку/разблокировку учётных записей, удаление пользователей и изменение их ролей. В ответ сервер предоставляет актуальный список пользователей с указанием ролей, статусов и дат создания. Предусмотрена защита от блокировки или удаления самого себя и других администраторов.
     Управление справочником врачей. Модуль предоставляет интерфейс для добавления, редактирования и деактивации врачей. Данные о специализациях, номерах кабинетов, образовании и стаже врачей передаются в ядро, которое синхронизирует их с базой данных. Клиентские приложения пациентов и администратора получают актуальный перечень врачей для записи на приём.
     Запись на приём. Рабочий модуль пациента и администратора. Входящий поток данных содержит выбранного врача, дату, временной слот и причину обращения. Ядро системы проверяет доступность слота, создаёт запись со статусом PENDING, бронирует выбранный слот и автоматически генерирует два напоминания о предстоящем приёме (за 24 часа и за 2 часа). Клиенту возвращается подтверждение с идентификатором созданной записи.
     Управление расписанием врачей. Модуль отображает недельную сетку слотов с цветовой индикацией занятости. Врач может блокировать отдельные слоты, что исключает их из доступных для записи. Ядро системы при каждом запросе возвращает актуальное состояние слотов, учитывая как бронирования пациентов, так и ручные блокировки.
     Медицинская карта и история визитов. Модуль обеспечивает ведение электронной медицинской карты. Врач заполняет протокол приёма (жалобы, осмотр, диагноз по МКБ-10, назначения), который передаётся в ядро и сохраняется в связанной таблице visit_details. Запись переводится в статус COMPLETED, слот освобождается, а напоминания отменяются. Пациент видит хронологию завершённых визитов и неявок с указанием диагнозов и назначений.
     Напоминания. Интеллектуальный компонент системы. При создании записи ядро автоматически формирует напоминания с заданными интервалами. Фоновый планировщик на сервере периодически сканирует таблицу напоминаний и для тех, чьё время отправки наступило, выполняет отправку уведомления и помечает их как отправленные (SENT). Пациент может просматривать свои напоминания, изменять интервал, канал или отключать их.
     Аналитика и отчётность. Модуль визуализации бизнес-показателей. Администратор выбирает тип отчёта, период и детализацию. Ядро системы на основе агрегированных данных из базы формирует CSV-файл с UTF-8 BOM для корректного отображения кириллицы, который сохраняется на компьютере администратора.
     Системные журналы. Модуль аудита всех критических действий. Каждая операция (вход, запись, отмена, сохранение диагноза) регистрируется ядром в таблице audit_log с указанием пользователя, типа действия, затронутой сущности и уровня важности (INFO, WARN, ERROR). Администратор имеет возможность просматривать журнал с фильтрацией по пользователю, действию и уровню, а также выполнять поиск по содержимому.
     Хранение и защита данных. Физический уровень архитектуры, реализованный на реляционной базе данных PostgreSQL. Ядро системы отправляет SQL-запросы через слой DAO. База данных выполняет запросы, обеспечивает ссылочную целостность и каскадное удаление связанных записей, а также возвращает извлечённые наборы данных для дальнейшей обработки серверной логикой.
     Определены особенности функционирования бизнес-процессов после внедрения разрабатываемого программного средства. В результате автоматизации будет создана единая информационная среда, обеспечивающая сквозной процесс записи пациента к врачу – от выбора специалиста и бронирования времени до сохранения медицинского протокола и получения назначений. Автоматическая генерация напоминаний и их фоновая отправка снижают частоту неявок и повышают дисциплину лечения. Многоуровневая система ролей и токенов гарантирует, что каждый пользователь видит только свою информацию, а административные действия протоколируются для последующего аудита. Тесная интеграция всех модулей позволяет исключить ручное управление расписанием, минимизировать ошибки при записи и существенно повысить эффективность работы регистратуры и врачей. Внедрение спроектированной архитектуры обеспечит высокую отказоустойчивость системы, надёжную защиту персональных данных и позволит руководству медицинского учреждения принимать эффективные управленческие решения на основе оперативной и достоверной аналитики.
     По результатам второй главы можно сформулировать следующие выводы. Разработанные UML-диаграммы обеспечивают полное формальное описание бизнес-процессов системы: от аутентификации пользователей и записи на приём до формирования отчётов и отправки напоминаний. Спроектированная реляционная база данных из одиннадцати нормализованных таблиц с внешними ключами и ограничениями целостности гарантирует корректное хранение и связность данных. Сформулированные функциональные требования охватывают полный жизненный цикл приёма, а нефункциональные требования закрепляют критерии безопасности, производительности и переносимости. Предложенный образ решения в виде клиент-серверной архитектуры на TCP/IP-сокетах с протоколом сериализованных команд и радиальной топологией десяти специализированных модулей создаёт исчерпывающую основу для проектирования и разработки программного средства[8].

          3 ПРОЕКТИРОВАНИЕ И РАЗРАБОТКА 
        ПРОГРАММНОГО СРЕДСТВА
     
      3.1 Архитектурные решения и технологии реализации 
            программного средства
     
     В процессе проектирования архитектуры разрабатываемого программного средства для автоматизации записи к врачу ключевым этапом являлся выбор модели реализации и взаимодействия компонентов системы. Были рассмотрены три основных архитектурных подхода: монолитное приложение, веб-ориентированная система и классическое клиент-серверное приложение. Сравнительный анализ данных моделей по ключевым критериям эффективности представлен в таблице 3.1.
     
Таблица 3.1 – Сравнительный анализ архитектурных моделей
Критерий сравненияМонолитное приложениеВеб-приложениеКлиент-серверное (TCP/IP)Независимость от интернет-каналаПолнаяКритичнаяПолнаяСкорость обработки транзакцийВысокаяЗависит от сетиВысокая (режим реального времени)Безопасность бизнес-логикиНизкаяСредняяВысокая (централизована)МасштабируемостьОграниченаВысокаяВысокая (пул потоков)Сложность развёртыванияПростаяСредняя (сервер + браузер)Простая (Docker Compose)Поддержка offline-режимаПолнаяОтсутствуетПолная      
      Анализ показал, что клиент-серверная архитектура на базе TCP/IP является оптимальной для системы контроля корпоративных расходов, так как обеспечивает максимальную скорость валидации транзакций в реальном времени, централизованную безопасность и независимость от качества внешнего интернет-соединения.
      Для реализации серверной части был выбран язык программирования Java 21 LTS. Выбор обусловлен статусом долгосрочной поддержки, мощной экосистемой, строгой типизацией, встроенной поддержкой многопоточности и современными языковыми конструкциями, которые значительно повышают надёжность и читаемость кода медицинской системы[11].
      Перечень применяемых технологий и библиотек приведён в 
таблице 3.2.
      
Таблица 3.2 – Технологический стек разрабатываемого
                         программного средства
Технология / БиблиотекаВерсияНазначениеJava21 LTSОсновной язык разработки серверного приложенияHibernate ORM6.4Объектно-реляционное отображение и управление транзакциямиPostgreSQL16Реляционная СУБДJackson Databind2.17JSON-сериализация протокола обменаLombok1.18Генерация boilerplate-кодаSLF4J / Logback2.0/1.5ЖурналированиеJUnit 55.10Модульное тестированиеDocker / Docker Compose25+ / 2.xКонтейнеризация и оркестрацияMaven3.9Сборка проектаТехнология / БиблиотекаВерсияНазначение     Обоснование выбора клиентского интерфейса. Для клиентской части рассматривались три варианта: полноценное десктопное приложение на JavaFX, Swing и консольный (командный) интерфейс. Сравнительный анализ представлен в таблице 3.3.
Таблица 3.3 – Сравнительный анализ вариантов клиентского интерфейса
Критерий сравненияJavaFXSwingКонсольный интерфейсСкорость разработкиСредняяНизкаяВысокаяКроссплатформенностьОтличнаяХорошаяОтличнаяРесурсоёмкостьВысокаяСредняяМинимальнаяПростота развёртыванияТребует JRE/JavaFXТребует JREНе требует GUI-библиотекСоответствие требованиям проектаИзбыточноУстаревшийОптимальноУдобство для администратораВысокоеСреднееДостаточное (для технических пользователей)     
     В результате анализа было выбрано полноценное десктопное приложение на JavaFX. Такое решение обусловлено следующими преимуществами: удобство работы с графическим интрефейсом[12] намного выше, чем с консольным, для коммерческого использования внешний вид приложения играет значимую роль, поэтому консольный интерфейс не подходит.
     Для выявления наиболее подходящей СУБД проведено сравнение популярных решений. Результаты представлены в таблице 3.1.
     
Таблица 3.1 – Сравнение СУБД
ХарактеристикаMySQLPostgreSQLSQLiteMicrosoft SQL Server12345Тип СУБДРеляционнаяОбъектно-реляционнаяВстраиваемая реляционнаяРеляционная (коммерческая)ЛицензияOpen Source (GPL)Open SourcePublic DomainПроприетарнаяACID-совместимостьПолная (InnoDB)ПолнаяПолнаяПолнаяПроизводитель-ностьВысокаяВысокаяВысокая (локальная)ВысокаяМасштабируе-мостьГоризонталь-наяГоризонталь-ная и вертикальнаяТолько локальнаяВертикальнаяБезопасностьРолевая модель, шифрованиеРасширенная (SELinux)Базовые механизмыИнтеграция с ADПопулярностьВеб/десктоп приложенияКорпоративные системыМобильные/деск-топКорпоративный сектор
     В качестве СУБД выбрана PostgreSQL[4]. Данная система демонстрирует отличные показатели как реляционная СУБД с открытым исходным кодом, полностью соответствующая требованиям ACID. PostgreSQL оптимизирована для широкого спектра операций, поддерживает горизонтальное масштабирование[14], предлагает надёжные механизмы безопасности, включая ролевую модель доступа и шифрование данных.
     Разработка программного обеспечения ведётся под операционной системой Windows, что обусловлено её доминирующим положением на рынке. Данная платформа обеспечивает бесшовную интеграцию с IntelliJ IDEA и нативную поддержку Java без необходимости дополнительной конфигурации.
     В качестве основной среды разработки применяется IntelliJ IDEA – функциональный инструмент с продвинутыми возможностями автодополнения кода, инструментами рефакторинга и комплексными средствами отладки[15]. Важным преимуществом является полная совместимость с JavaFX и встроенная поддержка системы сборки Maven, что оптимизирует управление зависимостями[16].
     Диаграмма развёртывания (Deployment Diagram) в нотации UML показывает физическое распределение компонентов системы. Клиентский узел содержит Java-приложение с GUI на JavaFX. Серверный узел содержит Java ServerSocket-приложение с бизнес-логикой и модулем напоминаний, а также СУБД PostgreSQL. Взаимодействие клиента и сервера осуществляется через TCP/IP-сокеты в локальной сети. Сервер взаимодействует с почтовым SMTP-сервером для отправки напоминаний пациентам.
     
     
     
     
     
Рисунок 3.1 – Диаграмма развёртывания

     Диаграмма компонентов системы включает следующие ключевые модули. На стороне клиента: графический интерфейс на JavaFX, включающий формы входа, записи к врачу, просмотра расписания; клиентский сокет, обеспечивающий сетевое соединение с сервером. На стороне сервера: серверный сокет, принимающий входящие подключения; обработчики запросов по типам: LoginHandler, AppointmentHandler, ReminderHandler, AdminHandler; слой бизнес-логики с сервисами: AppointmentService, ReminderService, UserService; DAO-слой (Data Access Objects) для работы с БД; база данных PostgreSQL.
     
      
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     

     
     Рисунок 3.2 – Диаграмма компонентов
     
     Данная диаграмма полностью отражает взаимодействие основных компонентов приложения между собой.
     
     3.2. Проектирование и разработка пользовательского интерфейса
     
     Пользовательский интерфейс представляет собой совокупность программных и аппаратных средств, обеспечивающих взаимодействие пользователя и вычислительной системы[17]. На начальном этапе разработки пользовательского интерфейса осуществляется распределение функций приложения по экранам, определяются макеты экранов и их содержимое; определяются элементы управления и их поведение.
     На начальном этапе разработки программного средства были разработаны прототипы пользовательского интерфейса, чтобы определить графическое оформление будущего приложения.
     При входе в систему (авторизации) перед пользователем будет отображаться окно входа, прототип которого представлен на рисунке 3.1.

     




















Рисунок 3.3 – Прототип страницы входа
     
     Главное окно пациента будет содержать таблицу с историей назначений от врачей, плашку с кнопкой для записи к врачу и ссылки для перехода на остальные страницы приложения. Пример такого окна представлен на рисунке 3.4.

Рисунок 3.4 – Прототип главной страницы пациента
    
     На странице каталога врачей будет представлен список всех доступных для записи врачей, поле для удобного поиска специалиста по имени или специальности а также фильтры для специализации и времени. Пример такого окна представлен на рисунке 3.5.
     
     
Рисунок 3.5 – Прототип страницы каталога врачей
     
     Записи пациента к врачам будут в отдельном окне, которое будет содержать действующие записи, поле для их поиска, фильтры, а так же плашки с количеством предстоящих, завершённых, отменённых записей и неявок. Пример такого окна представлен на рисунке 3.6.
     
     
Рисунок 3.6 – Прототип страницы записей пациента

     Эти прототипы представляют собой начальную визуализацию интерфейса приложения. На текущем этапе они выполняют несколько важных функций в процессе проектирования: во-первых, помогает структурировать основные разделы приложения, включая главную страницу с логотипом, навигационное меню и раздел истории посещений; во-вторых, позволяет предварительно оценить логику взаимодействия пользователя с системой. 
     Такой макет, несмотря на свою простоту, уже позволяет проанализировать несколько аспектов будущего приложения: удобство навигации, достаточность предоставляемой информации в разделе заказов и пользователей, необходимость дополнительных функциональных элементов. 
     Дальнейшее развитие этих прототипов должно включать несколько направлений работы: добавление дополнительных интерактивных элементов управления (кнопки, фильтры, формы), расширение системы навигации (дополнительные меню, ссылки), введение визуальных акцентов для улучшения восприятия информации, проработку состояний интерфейса для различных сценариев использования, а также другие страницы для реализации поставленных задач.
     
     3.3 Разработка модели данных
     
     Схема базы данных представляет собой фундаментальный каркас, который определяет архитектуру хранения информации. Она включает в себя совокупность таблиц, их атрибутов, взаимосвязей между ними, а также накладываемых ограничений и спецификаций типов данных. Этот концептуальный макет выполняет роль технического руководства, регламентирующего принципы хранения и обработки данных в системе.
     Физическая схема, изображенная на рисунке 3.7, представляет фактическую структуру физического хранилища базы данных на диске. Он включает в себя механизмы хранения, файловые структуры, индексы и другие детали низкого уровня, которые определяют, как данные хранятся и получают к ним доступ со стороны базовой системы управления базами данных (СУБД). Этот тип схемы в основном связан с эффективным использованием пространства хранения, повышением производительности базы данных за счет оптимизации операций поиска и хранения данных.
     

     
Рисунок 3.7 – Физическая модель представления базы данных
     Логический уровень, представленный на рисунке 3.10, выступает промежуточным звеном между физической реализацией и концептуальным представлением. Он описывает данные в терминах таблиц, полей, связей и ограничений, формируя четкую структуру, понятную разработчикам приложений. На этом уровне определяются первичные и внешние ключи, типы данных, триггеры и хранимые процедуры. Логическая схема не зависит от способа физического хранения, что позволяет изменять параметры производительности без переделки структуры таблиц.
     

     
Рисунок 3.8 – Логическая модель представления базы данных

     Концептуальный уровень представляет собой наиболее абстрактное представление данных, ориентированное на бизнес-пользователей. Он фокусируется на сущностях предметной области, их атрибутах и взаимосвязях, игнорируя технические детали реализации. Этот уровень служит общим языком общения между заказчиками, аналитиками и разработчиками, помогая корректно отразить бизнес-процессы в структуре базы данных. Здесь важны не технические параметры, а смысловая нагрузка данных и их роль в бизнес-логике. Такая модель изображена на рисунке 3.9.


Рисунок 3.9 –  Концептуальная модель представления базы данных

     Представленные схемы базы данных предназначены для приложения записи к врачу. Оно позволяет эффективно управлять расписанием врачей, пользователями системы и процессом обработки записи. Рассмотрим каждую таблицу и их взаимосвязи более подробно.
     Таблица пользователей (users). Центральной таблицей системы является таблица пользователей, хранящая учётные данные всех участников – пациентов, врачей и администраторов. Каждый пользователь имеет уникальный числовой идентификатор (id). Поле login содержит электронную почту, которая используется как уникальное имя для входа в систему. В целях безопасности пароль хранится в виде хэш-суммы (password_hash), что исключает возможность чтения исходного пароля даже при компрометации базы данных. Дополнительное поле salt усиливает защиту паролей. Роль пользователя (role) определяет его права и доступный интерфейс: пациент записывается на приёмы и просматривает свою историю, врач работает с расписанием и заполняет протоколы, администратор управляет справочниками и аудитом. Флаг blocked позволяет временно или навсегда заблокировать учётную запись без её удаления, сохраняя все связанные данные. Дата создания (created_at) фиксирует момент регистрации.
     Таблица сессий (sessions). Для поддержания безопасного доступа после входа пользователя в систему создаются сессии. Каждая сессия привязана к конкретному пользователю (user_id) и содержит уникальный токен (token), который передаётся клиентом при каждом запросе к серверу вместо повторной отправки пароля. Сессия имеет ограниченный срок жизни (expires_at), после которого токен становится недействительным. IP-адрес (ip_adress) сохраняется для дополнительного аудита безопасности. Таблица связана с users каскадным удалением – при удалении пользователя все его сессии автоматически уничтожаются.
     Таблица специализаций (specializations). Справочник специализаций является простой таблицей-словарём, содержащей идентификатор и название специализации (например, «Терапевт», «Стоматолог»). Она служит для нормализации данных врачей и позволяет легко расширять перечень медицинских направлений без изменения структуры других таблиц.
     Таблица врачей (doctor). Профиль врача расширяет учётную запись пользователя медицинской информацией. Связь с таблицей users осуществляется через внешний ключ user_id, что позволяет врачу входить в систему со своими персональными данными. Поле full_name хранит полное имя врача, отображаемое в каталоге и интерфейсе пациента. Специализация (specialization_id) связывает врача с конкретным направлением. Номер кабинета (office_number) и данные об образовании (education) дают пациенту дополнительную информацию. Поле about может содержать свободное описание квалификации. Стаж (experience_years) и стоимость приёма (price) используются при сортировке и фильтрации врачей в каталоге. Флаг active позволяет отключать врача из доступных для записи, не удаляя его исторические данные.
     Таблица пациентов (patients). Аналогично врачам, профиль пациента дополняет запись в users персональными медицинскими данными. Связь user_id является обязательной и уникальной для каждого пациента. Помимо полного имени (full_name) хранятся контактные данные (phone, email), дата рождения для определения возраста, номер полиса ОМС (policy), информация об аллергиях (allergy) и группа крови (blood_type). Эти сведения отображаются в электронной медицинской карте (ЭМК) при приёме врача. Каскадное удаление гарантирует целостность: при удалении пользователя-пациента удаляется и его профиль.
     Таблица временных слотов (schedule_slots). Расписание врачей представлено слотами – минимальными временными интервалами, доступными для записи. Каждый слот относится к конкретному врачу (doctor_id) и содержит дату (slot_date), время начала (start_time) и окончания (end_time). Флаг booked показывает, занят ли слот в данный момент; при отмене или завершении приёма слот автоматически освобождается. Флаг blocked используется врачом или администратором для временного блокирования времени (например, на учёбу или совещание). Уникальность слота гарантируется составным ключом по врачу, дате и времени начала, что исключает дублирование.
     Таблица записей на приём (appointments). Эта таблица объединяет пациента, врача и выбранный слот в одну запись о приёме. Связи с patients (через patient_id) и doctors (через doctor_id) являются обязательными. Связь со слотом (slot_id) опциональна – слот освобождается при отмене записи, но история сохраняется. Дата и время приёма (appointment_datetime) фиксируются как единое значение, независимо от слота. Статус (status) проходит жизненный цикл: PENDING (ожидает подтверждения), CONFIRMED (подтверждено), COMPLETED (приём завершён), либо CANCELLED (отменено) или MISSED (неявка). Причина обращения (reason) вводится пациентом при записи. Дата создания (created_at) используется для аналитики.
     Таблица деталей визита (visit_details). При завершении приёма врач заполняет медицинский протокол, который сохраняется в таблице visit_details. Она связана один-к-одному с записью (appointment_id). В протокол входят жалобы пациента (complaints), результаты объективного осмотра (examination), код диагноза по МКБ-10 (diagnosis_code) и его текстовое описание (diagnosis_text), выписанные назначения (prescription)и дополнительные заметки врача (notes). Эти данные немедленно становятся доступны пациенту в его медицинской карте и истории визитов, а также отражаются в истории назначений на главной панели пациента.
     Таблица напоминаний (reminders). Для каждого созданного приёма автоматически генерируются напоминания, которые хранятся в этой таблице. Каждое напоминание ссылается на конкретный приём (appointment_id). Время отправки (scheduled_send_time) рассчитывается как разница между временем приёма и интервалом (interval_minutes), например, за 1440 (24 часа) или 120 минут. Канал связи (channel) определяет способ отправки (Email, Push, SMS). Статус (SCHEDULED, SENT, CANCELLED) отслеживает жизненный цикл уведомления. Флаг enabled позволяет отключить конкретное напоминание без удаления. При отмене приёма все запланированные напоминания автоматически переходят в статус CANCELLED.
     Таблица журнала аудита (audit_log). Все критические действия в системе регистрируются в таблице аудита. Для каждого события сохраняется идентификатор пользователя (user_id), его логин, тип действия (action), тип затронутой сущности (entity_type), её идентификатор (entity_id), человекочитаемое описание (details), уровень важности (level: INFO, WARN, ERROR) и временная метка (timestamp). Связь с users не является жёстким внешним ключом, что позволяет сохранять записи даже после удаления пользователей. Эта таблица используется администратором для мониторинга и расследования инцидентов.
     Таблица настроек уведомлений (notification_settings). Каждый пользователь может настроить глобальные параметры получения напоминаний. Таблица связана один-к-одному с users через user_id. Флаги channel_email, channel_push, channel_sms активируют соответствующие каналы. Интервалы interval_24h, interval_2h, interval_30min определяют, за какое время до приёма отправлять уведомления. Чтобы не беспокоить пользователя в ночное время, можно включить «тихие часы» (quiet_hours_enabled)и задать их границы (quiet_from, quiet_to).
     Центральными связующими таблицами являются users, doctors, patients и appointments. Пользователь после регистрации получает роль; в зависимости от роли создаётся связанная запись либо в doctors, либо в patients. Это позволяет врачу и пациенту использовать один механизм аутентификации (через sessions), но видеть разные интерфейсы и данные.
     Запись на приём (appointments) связывает пациента и врача через временной слот (schedule_slots). При успешной записи слот помечается занятым (booked=true), и создаются напоминания в таблице reminders. После завершения приёма врач заполняет visit_details, статус записи становится COMPLETED, слот освобождается, а напоминания переходят в CANCELLED. Вся эта информация немедленно отражается в интерфейсах: пациент видит назначения в «Истории назначений» и в «Медицинской карте», врач – завершённый приём в своей истории.
     Архитектура системы построена по классической клиент-серверной модели. Клиентское приложение (JavaFX) отправляет команды (Command) на сервер, который через слой DAO взаимодействует с базой данных. Сессии поддерживаются токенами, что исключает необходимость постоянной передачи пароля. Ролевой доступ (PATIENT, DOCTOR, ADMIN) ограничивает видимые функции: пациент не может редактировать расписание врача, а врач не может записываться на приём. Все модификации критических данных (создание/отмена записи, сохранение диагноза) протоколируются в AUDIT_LOG, обеспечивая прослеживаемость действий[8].
     Такая нормализованная структура базы данных гарантирует целостность информации, исключает дублирование и позволяет масштабировать функциональность системы независимо в разных компонентах – будь то добавление новых специализаций, каналов напоминаний или расширение медицинского протокола.
     На рисунке 3.10 представлена UML-дирамма классов серверной части приложения, которая отражает структуру программных компонентов, их свойства, методы и взаимосвязи между ними. На диаграмме представлены основные классы, отвечающие за обработку пользователей, врачей, пациентов, расписания, записей на приём, уведомлений и взаимодействия с базой данных.
     Центральное место в архитектуре занимают классы, связанные с обработкой пользователей системы. Класс User описывает общую модель пользователя и содержит данные авторизации, роли и состояния учётной записи. С ним связаны классы, отвечающие за хранение сессий пользователей и настройку уведомлений. В системе предусмотрены различные роли пользователей: пациент, врач и администратор.
     Класс Doctor реализует модель врача и содержит сведения о специализации, стаже работы, стоимости приёма, кабинете и дополнительной информации. Класс связан с сущностью специализации (Spezialisation) и используется при формировании расписания и создании записей на приём. Для хранения информации о пациентах используется класc Patient, содержащий персональные данные, контактную информацию и медицинские сведения.
     Для организации расписания врачей используется клаcc ScheduleSlot, описывающий временные интервалы приёма. Каждый слот связан с конкретным врачом и содержит информацию о дате, времени начала и окончания, а также о доступности слота для записи.
     Процесс записи пациента к врачу реализуется посредством класса Appointment. Данный класс связывает пациента, врача и выбранный временной слот. В объекте записи хранится дата и время приёма, причина обращения и текущий статус записи. Для хранения результатов приёма и медицинских данных используется класс VisitDetails, содержащий жалобы пациента, результаты осмотра, диагноз, назначения и комментарии врача.
     Модуль напоминаний реализован с помощью класса Reminder. Он хранит информацию о времени отправки уведомления, используемом канале связи (Email, SMS или Push-уведомление), статусе отправки и параметрах активности напоминания. Дополнительно реализован класс NotificationSettings, позволяющий пользователю настраивать каналы уведомлений, интервалы отправки и режим тихих часов.
     Для регистрации действий пользователей и отслеживания событий системы используется класс AuditLog. В журнале аудита сохраняются сведения о выполненных операциях, типе сущности, уровне события и времени его возникновения.
     

     Рисунок 3.10 – UML-диаграмма классов
     
     На диаграмме также представлены сервисные и инфраструктурные классы серверной части приложения. Они обеспечивают обработку клиентских запросов, выполнение бизнес-логики, взаимодействие с базой данных PostgreSQL и обмен данными между клиентом и сервером. Между классами реализованы отношения ассоциации, зависимости, наследования и композиции, что отражает взаимодействие компонентов системы и распределение ответственности между ними.
     
     3.4 Описание статических и динамических аспектов поведения 
           программных объектов
     
     Диаграмма состояний, изображенная на рисунке 3.10, демонстрирует последовательность состояний, в которых будет находиться автоматизированная система управления складскими запасами в процессе добавления данных о пользователях или товарах.


     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     

Рисунок 3.10 – Даиграмма состояний

     Начальным состоянием процесса является нахождение пользователя в каталоге врачей. После выбора врача открывается диалог записи на приём. В данном состоянии отображается мини-карточка выбранного врача, календарь для выбора даты приёма, поле для ввода причины обращения и кнопка «Записаться», которая изначально неактивна.
     После выбора даты в календаре приложение переходит в состояние загрузки свободных временных слотов. На этом этапе клиентское приложение отправляет серверу запрос GET_SLOTS, содержащий идентификатор врача и выбранную дату. Во время ожидания ответа комбобокс выбора времени переводится в состояние загрузки и отображает соответствующее сообщение.
     После получения ответа от сервера система переходит в состояние отображения загруженных слотов. Если на выбранную дату существуют свободные интервалы времени, комбобокс становится активным, а первый доступный слот выбирается автоматически. Если свободных слотов нет, комбобокс блокируется, а пользователю отображается сообщение о недоступности записи на выбранную дату.
     Далее пользователь выбирает удобное время приёма и вводит причину обращения. После нажатия кнопки «Записаться» приложение переходит в состояние отправки запроса на сервер. На данном этапе формируется команда BOOK_APPOINTMENT, содержащая идентификатор врача, выбранного слота, дату и время приёма, а также текст причины обращения. Во время отправки кнопка записи становится неактивной для предотвращения повторной отправки запроса.
     После отправки данных система переходит в состояние ожидания ответа сервера. По завершении обработки сервер возвращает результат выполнения операции, после чего приложение переходит в состояние обработки ответа.
     Если сервер возвращает успешный результат (OK), система переходит в состояние успешного создания записи. Пользователю отображается уведомление об успешной записи на приём, диалоговое окно закрывается, а список «Мои записи» автоматически обновляется.
     Если сервер возвращает ошибку (CLIENT_ERROR или SERVER_ERROR), приложение переходит в состояние отображения ошибки. В данном состоянии пользователю выводится сообщение с описанием проблемы, после чего интерфейс возвращается к диалогу записи, а кнопка «Записаться» вновь становится активной, позволяя повторить попытку записи.
     На рисунке 3.11 представлена диаграмма последовательности для процесса записи пациента к врачу в клиент-серверной системе.
     Пациент начинает с выбора врача через интерфейс «Каталог врачей», после чего переходит к диалогу записи, где выбирает дату и ожидает загрузки свободных временных слотов. Система отправляет на сервер запрос с параметрами врача и выбранной даты; сервер, получив запрос, обращается к базе данных для получения списка незанятых и незаблокированных слотов. Получив подтверждение о наличии свободного времени, сервер возвращает клиенту список доступных интервалов, и клиент отображает их в виде выпадающего списка. Пользователь выбирает подходящее время, указывает причину обращения и подтверждает запись. Клиентское приложение отправляет запрос на бронирование, содержащий идентификаторы пациента, врача, слота, а также причину и дату. Сервер проверяет корректность переданных данных и наличие профиля пациента, после чего создаёт запись в базе данных со статусом «Ожидает», одновременно помечая выбранный слот как занятый и регистрируя событие в журнале аудита. На следующем этапе сервер генерирует два автоматических напоминания о предстоящем приёме (за 24 часа и за 2 часа), сохраняя их в соответствующей таблице. Завершив обработку, сервер возвращает клиенту успешный ответ, а клиент уведомляет пользователя об успешной записи и обновляет интерфейс раздела «Мои записи». В дальнейшем фоновый планировщик периодически проверяет наличие запланированных напоминаний с наступившим временем отправки и имитирует их доставку, фиксируя статус «Отправлено».


Рисунок 3.11 – Диаграмма последовательности

     На рисунке 3.12 представлена диаграмма деятельности для записи к врачу.
     
     
     
Рисунок 3.12 – Диаграмма деятельности
     3.5 Разработка и описание алгоритмов, реализующих бизнес-логику 
           программного средства
     
     Алгоритм, изображенный на рисунке 3.13, описывает процесс входа в систему с авторизацией или регистрацией пользователей, а также последующее взаимодействие в зависимости от роли.
     При запуске приложения пользователь попадает на экран входа, где ему предлагается два варианта действий: авторизоваться под существующей учётной записью или создать новую. При выборе регистрации открывается форма с полями для ввода полного имени, адреса электронной почты, пароля и его подтверждения. Клиентская часть выполняет валидацию введённых данных: проверяется заполнение всех полей, корректность формата email, длина пароля не менее восьми символов, а также совпадение пароля и его подтверждения. Если валидация не пройдена, соответствующие поля подсвечиваются, и пользователь остаётся на форме до исправления ошибок. При успешной валидации клиент вычисляет хеш пароля по алгоритму SHA-256 и отправляет на сервер команду REGISTER, содержащую полное имя, email и хеш[9]. Сервер проверяет, не занят ли указанный email, и при отрицательном результате создаёт новую запись в таблице пользователей с ролью PATIENT, а также связанную с ней запись в таблице пациентов. После успешного создания учётной записи сервер возвращает клиенту токен сессии, идентификатор пользователя и роль. Клиент, получив ответ, уведомляет пользователя об успешной регистрации и автоматически выполняет вход, открывая главное окно пациента. Если же сервер сообщает о том, что email уже используется, клиент отображает ошибку рядом с полем email и возвращает пользователя к редактированию.
     При выборе авторизации открывается форма с полями email и пароля. Клиент проверяет заполнение полей и длину пароля, после чего отправляет команду LOGIN, передавая email и хеш пароля. Сервер ищет пользователя с указанным email и сравнивает хеш; если учётная запись не найдена, пароль не совпадает или учётная запись заблокирована, сервер возвращает ошибку авторизации. Клиент, получив такой ответ, отображает сообщение «Неверный email или пароль» и даёт возможность повторить ввод. В случае успешной аутентификации сервер создаёт новую сессию, сохраняет её в базе данных и возвращает клиенту токен, идентификатор пользователя, роль и полное имя. Клиент, проанализировав значение роли, выполняет перенаправление: для роли PATIENT загружается главная страница пациента с персональным приветствием, для DOCTOR – рабочее окно врача с его реальной специализацией, а для ADMIN – интерфейс администратора с панелью управления.
Рисунок 3.13 – Схема алгоритма клиент-серверного взаимодействия

     Алгоритм на рисунке 3.14 описывает процесс записи пациента к выбранному врачу с проверкой свободных дат и временных слотов данного специалиста.
     
Рисунок 3.14 – Схема алогритма записи к врачу
     Процесс начинается с перехода авторизованного пациента в раздел «Каталог врачей». Клиент отправляет запрос GET_ALL_DOCTORS, получает от сервера список активных врачей и отображает их в виде карточек. Пациент выбирает нужного врача и нажимает кнопку «Записаться», после чего открывается диалог записи с календарём, полем причины обращения и неактивной кнопкой подтверждения.
     Далее пациент выбирает дату в календаре (доступны только дни начиная с завтрашнего). Клиент отправляет серверу команду GET_SLOTS с идентификатором врача и выбранной датой. Сервер обращается к базе данных и получает список слотов, у которых флаги BOOKED и BLOCKED установлены в FALSE. На этом этапе процесс разветвляется:
     Если свободные слоты отсутствуют, клиент отображает сообщение «Нет свободных слотов», комбобокс времени остаётся заблокированным, и пациент возвращается к выбору другой даты.
     Если слоты найдены, клиент заполняет выпадающий список доступными временными интервалами и активирует кнопку «Записаться». Пациент выбирает подходящее время, вводит причину обращения и подтверждает запись.
     При нажатии кнопки «Записаться» клиент формирует команду BOOK_APPOINTMENT, передавая идентификаторы врача, слота, пациента, а также дату, время и причину. Сервер выполняет следующие операции: создаёт запись в таблице appointments со статусом PENDING, помечает выбранный слот как занятый (booked = true), генерирует два напоминания: за 24 часа и за 2 часа до приёма регистрирует событие в журнале аудита.
     После успешного выполнения всех операций сервер возвращает клиенту ответ со статусом OK. Клиент отображает уведомление «Запись успешно создана», закрывает диалог записи и обновляет раздел «Мои записи», где новый приём появляется во вкладке «Предстоящие». Процесс завершается.
     Таким образом, диаграмма деятельности наглядно демонстрирует последовательность действий, условия ветвления и взаимодействие компонентов системы, обеспечивающих корректную запись пациента на приём.
     
     
Рисунок 3.15 – Схема алгоритма формирования отчётов

     Диаграмма деятельности описывает алгоритм генерации аналитических отчётов администратором. Процесс начинается с перехода администратора в раздел «Отчёты», где отображаются четыре карточки типов отчётов: «Нагрузка по врачам», «Статистика явки», «Эффективность напоминаний» и «Активность пациентов». Администратор выбирает нужный тип отчёта и нажимает кнопку «Сформировать», после чего открывается диалоговое окно с параметрами. В диалоге пользователь указывает период отчёта, формат (CSV) и детализацию (по дням, неделям или месяцам). При нажатии кнопки «Сформировать» выполняется проверка корректности дат: если даты не указаны или конечная дата предшествует начальной, отображается предупреждение, и пользователь возвращается к редактированию параметров. Если даты указаны верно, система открывает стандартный диалог сохранения файла, позволяя выбрать место и имя файла.
     После выбора места сохранения начинается генерация файла. Создаётся CSV-файл с кодировкой UTF-8 и обязательной записью метки порядка байтов для корректного отображения кириллицы в Microsoft Excel[23]. В файл последовательно записываются: заголовок отчёта с его названием, период и детализация, а затем данные, соответствующие выбранному типу. Для отчёта «Нагрузка по врачам» подсчитывается количество приёмов для каждого врача; для «Статистики явки» – количество неявок в разрезе специализаций; для «Эффективности напоминаний» – общее количество, количество отправленных и запланированных уведомлений; для «Активности пациентов» – частота обращений каждого пациента. Источником данных служат уже загруженные в память списки записей и напоминаний, полученные администратором при входе в систему.
     После формирования содержимого файл сохраняется на диск. Если сохранение прошло успешно, администратору отображается уведомление с указанием полного пути к сохранённому файлу. В случае ошибки записи на диск выводится сообщение об ошибке. Процесс завершается закрытием диалога и возвратом к списку отчётов. Таким образом, диаграмма демонстрирует последовательность действий пользователя, логику проверки входных данных и взаимодействие с файловой системой, обеспечивающее получение отчётов в машиночитаемом формате.
     
     3.6 Механизмы обеспечения информационной безопасности
     
     Разработанная система управления складскими запасами реализует комплексный подход к информационной безопасности, соответствующий современным стандартам защиты данных. В основе системы лежит многоуровневая архитектура безопасности, включающая несколько взаимодополняющих механизмов защиты.
     Ключевым элементом системы является строгая процедура аутентификации и авторизации с разделением пользователей на три категории: пациенты, врачи и администраторы. Каждой категории назначены четко определенные права доступа, что исключает возможность несанкционированного выполнения операций. Процесс регистрации включает обязательную верификацию контактных данных для предотвращения создания фальшивых аккаунтов.
     Для защиты передаваемых данных используется протокол TCP, обеспечивающий надежную доставку и целостность информации. Дополнительно реализованы механизмы контроля последовательности пакетов и проверки их подлинности. Критически важные операции, такие как авторизация, защищены современными алгоритмами шифрования.
     Хранение учетных данных организовано с применением криптографических методов[20]. Пароли пользователей хранятся в хешированном виде с использованием уникальной «соли» для каждой учетной записи, что делает невозможным восстановление исходных паролей даже при компрометации базы данных.
     Система включает надежную защиту от SQL-инъекций через использование параметризованных запросов к базе данных[21]. Дополнительно реализована многоуровневая валидация и фильтрация входных данных на уровне приложения.
     Таким образом, система обеспечивает комплексную защиту данных на всех уровнях впв – от аутентификации пользователей до хранения информации, что позволяет эффективно противостоять современным угрозам информационной безопасности.


    4 ТЕСТИРОВАНИЕ И ПРОВЕРКА 
       РАБОТОСПОСОБНОСТИ ПРОГРАММНОГО 
       СРЕДСТВА
     
     Тестирование программного обеспечения представляет собой обязательный и критически важный этап разработки приложения для автоматизации записик врачу с модулем напоминаний пациентам[22]. Основные цели тестирования включают:  
        1 Проведение всесторонней проверки работы всех модулей при различных сценариях эксплуатации позволяет гарантировать стабильную и бесперебойную работу системы в реальных условиях. Тестирование включает обработку некорректных данных, проверку отказоустойчивости и восстановления после сбоев.  
        2 Комплексное тестирование механизмов авторизации, разграничения доступа и шифрования данных обеспечивает защиту конфиденциальной информации. Проверяется устойчивость системы к потенциальным угрозам, таким как SQL-инъекции или несанкционированный доступ. Также тестирование интерфейсов направлено на выявление и устранение проблем, которые могут затруднить взаимодействие сотрудников с системой. Проверяется удобство навигации, понятность форм ввода данных и корректность отображения отчетов.  
     Сообщение на рисунке 4.1 о пустых полях авторизации появляется, когда пользователь пытается войти в систему, не заполнив логин или пароль. Оно предотвращает отправку неполных данных и требует заполнить оба обязательных поля.
     












Рисунок 4.1 – Ошибка авторизации с незаполненными полями

     При вводе неверной комбинации логина и пароля система показывает сообщение об ошибке без уточнения, какой именно параметр введен неправильно – это сделано в целях безопасности. Пользователю следует проверить вводимые данные. Данная ошибка представлена на рисунке 4.2.










Рисунок 4.2 – Ошибка при вводе неправильных данных

При отсутствии подключения к серверу появляется сообщение, указывающее на эту ошибку. Пример такого сообщения показан на рисунке 4.3









Рисунок 4.3 – Ошибка подключения к северу

     При попытке записаться к врачу, не выбрав дату и время, появляется сообщение, подсказывающее, что нужно указать дату и время визита. Пример на рисунке 4.3.
     
     









Рисунок 4.4 – Ошибка при неверном выборе данных приёма

     При попытке сформировать отчёт администратором не выбрав начальную и конечеую дату периода, появляется соббщение об ошибке. Пример такого сообщения приведён на рисунке 4.5.
     
     
Рисунок 4.5 – Ошибка выбора дат периода отчёта

     Также в приложении предусмотрена обработка логической ошибки при попытке блокировки администратора им самим или другим администратором. Пример сообщения об ошибке представлена на рисунке 4.6
     
     
Рисунок 4.6 – Ошибка блокировки администратора

     Как видно, в приложении предусмотрена обработка всех возможных ошибок пользователя, что говорит о высокой работоспособности и надёжности системы.


    5 РУКОВОДСТВО ПО УСТАНОВКЕ 
       (РАЗВЕРТЫВАНИЮ) ПРОГРАММНОГО СРЕДСТВА
     
5.1 Руководство по установке (развертыванию)
      программного средства 
     
     Для работы приложения требуется компьютер с операционной системой Windows 10 или 11 (64разрядная версия) либо аналогичный по производительности сервер на базе Linux. Минимальные системные требования включают процессор Intel Core i3 или эквивалентный, 4 ГБ оперативной памяти и 2 ГБ свободного места на жёстком диске. Убедитесь, что на компьютере установлена актуальная версия Java Development Kit (JDK) не ниже 17; проверить её можно, открыв командную строку и выполнив команду java -version. Для серверной части также потребуется система управления базами данных PostgreSQL версии 14 или выше.
     Загрузите установочный пакет PostgreSQL с официального сайта и запустите инсталлятор. Во время установки выберите компоненты «PostgreSQL Server» и «pgAdmin 4» (графический интерфейс управления). В качестве типа конфигурации укажите «Development», метод аутентификации оставьте по умолчанию и задайте надёжный пароль для пользователя postgres; в нашем проекте по умолчанию используется пароль 1234. Порт подключения оставьте стандартным (5432) и завершите установку.
     
     
Рисунок 5.1 – Окно при завершении установки СУБД
     
     После установки откройте pgAdmin 4, создайте новое подключение к локальному серверу, указав адрес 127.0.0.1, порт 5432 и учётные данные postgres. В SQL-редакторе выполните команды указанные в Приложении В. Пример представлен на рисунке 5.2.
     
     
Рисунок 5.2 – Создание таблиц

     После успешного создания базы данных добавьте в серверный проект библиотекудрайвер PostgreSQL (JARфайл postgresql-42.7.3.jar, загружаемый с jdbc.postgresql.org)[6]. В IntelliJ IDEA драйвер подключается через «File, Project Structure, Libraries, Add JAR». Затем откройте класс Main в пакете по умолчанию серверной части и запустите его. В консоли должно появиться сообщение «[DB] Соединение с БД установлено. Сервер запущен на порту 5555», что означает готовность сервера принимать клиентские подключения[25].
     
     
 Рисунок 5.3 – Сообщение на консоли после запуска серверной части
     
     Клиентская часть требует Java Runtime Environment 17+ и библиотеки JavaFX. При первом входе под врачом или администратором необходимо вручную создать соответствующие учётные записи через SQLзапросы; для врачей также потребуется сгенерировать временные слоты, выполнив скрипт генерации, который добавляет интервалы на нужные даты. Типовые проблемы – отсутствие соединения с сервером (проверьте, что он запущен и порт 5555 открыт), пустой список врачей или отсутствие свободных слотов (выполните скрипты наполнения) и ошибки с JavaFX (убедитесь, что прописаны правильные модули). После выполнения всех шагов приложение готово к использованию.
     
     
     
     
     
     
Рисунок 5.4 – Сообщение на консоли после запуска клиентской части
     
     5.2 Руководство пользователя
     
     Доступ к программному средству имеют три категории пользователей, а именно пациент, врач и администратор. Для начала работы с приложением пациенту необходимо создать учётную запись. На стартовом экране регистрации, который представлен на рисунке 5.5, в форме нужно заполнить поля «Полное имя», «Электронная почта», «Пароль» и «Подтверждение пароля». 
     
     
Рисунок 5.5 – Окно регистрации
     После нажатия кнопки «Зарегистрироваться» система выполнит проверку введённых данных; при обнаружении ошибок поля будут подсвечены красным, а при успешной регистрации появится информационное окно, представленное на рисунке 5.6 и пациент автоматически попадёт на главную страницу.
     
     
Рисунок 5.6 – Уведомление об успешно регрстрации
     
     После входа в систему пользователь видит главное окно пациента, представленное на рисунке 5.7. В верхней панели отображается персональное приветствие с именем пользователя. В левой части расположена боковая панель навигации с пунктами «Главная», «Мои записи», «Медицинская карта», «Каталог врачей» и «Напоминания». Центральную область занимают карточки: «История назначений», в которой перечислены ранее завершённые приёмы с указанием диагноза и назначений, и «Новая запись» с кнопкой быстрого перехода в каталог врачей.
     
     
     
     
     
     
     
     
     
     
     
     
     
     
     
Рисунок 5.7 – Главное окно пациента
     Для просмотра списка доступных врачей пациент переходит в раздел «Каталог врачей», скриншот которог представлен на риснке 5.8.
     
     
Рисунок 5.8 – Каталог врачей
     
     В открывшемся окне отображаются карточки активных специалистов с указанием имени, специализации, стажа, рейтинга и стоимости приёма. При необходимости список можно отфильтровать по специализации с помощью выпадающего списка «Все специализации» или отсортировать по рейтингу, стажу или цене. Чтобы записаться на приём, следует нажать кнопку «Записаться» на карточке выбранного врача – откроется диалог записи, представленный на рисунке 5.9.
     
     
Рисунок 5.9 – Диалог записи к врачу

     В этом окне представлена краткая информация о враче, календарь для выбора даты (доступны только дни, начиная с завтрашнего), выпадающий список времени и поле для причины обращения. Сначала необходимо выбрать дату – система загрузит свободные временные слоты и отобразит их в выпадающем списке. Если свободных слотов нет, появится соответствующее сообщение и следует выбрать другую дату. Выбрав время и указав причину обращения, пациент нажимает кнопку «Записаться». В случае успеха появится подтверждение, представленное на рисунке 5.10, а новая запись немедленно отобразится в разделе «Мои записи».
     
     
Рисунок 5.10 – Окно создания успешной записи
     Раздел «Мои записи» содержит три вкладки: «Предстоящие», «Прошедшие» и «Отменённые». Каждая запись представлена карточкой с указанием специализации, имени врача, даты, времени и статуса. Для предстоящих записей доступны кнопки «Подробнее» и «Отменить». При нажатии «Отменить» появляется окно подтверждения; после подтверждения запись перемещается во вкладку «Отменённые», а временной слот врача освобождается. В верхней части окна расположены плашки статистики, показывающие актуальное количество предстоящих, завершённых, отменённых записей и неявок. Скриншот рабочего окна представлен на рисунке 5.11.
     
     
Рисунок 5.11 – Окно «Мои записи»
     
     Завершённые приёмы автоматически попадают в «Медицинскую карту», скриншот которой представлен на рисунке 5.12. На этой странице пациент видит хронологию своих визитов, сгруппированную по годам и месяцам. Каждая запись содержит дату, специализацию, имя врача, статус («Завершён» или «Неявка»), а для завершённых приёмов – диагноз и назначения, выписанные врачом. При клике на запись открывается подробное окно с полной информацией о визите.
     
     
Рисунок 5.12 – Окно «Медицинская карта»
     
     Раздел «Напоминания», представленный на рисунке 5.12, показывает все уведомления о предстоящих приёмах, сгруппированные по врачу и дате. Для каждого напоминания отображаются запланированное время отправки, канал связи (Email), интервал (за 24 часа или за 2 часа) и статус. В верхней части страницы расположена карточка настроек уведомлений, позволяющая выбрать каналы и интервалы отправки. Пациент может отредактировать существующее напоминание (изменить интервал или канал) или отменить его, нажав соответствующую кнопку на карточке. Фильтры и поле поиска помогают быстро найти нужное уведомление.
     
     
Рисунок 5.13 – Окно «Напоминания»
     
     Таким образом, пациент имеет полноценный инструментарий для самостоятельной записи на приём, просмотра истории посещений, управления напоминаниями и получения назначений от врача – все данные загружаются с сервера и являются персонализированными для каждого пользователя.


ЗАКЛЮЧЕНИЕ
     
     В условиях активной цифровизации медицинских услуг разработанное приложение для автоматизации записи к врачу с модулем напоминаний представляет собой современное и эффективное решение для оптимизации процессов взаимодействия пациентов с медицинским персоналом. Проведённый проект охватил полный цикл разработки программного обеспечения – от глубокого анализа предметной области и изучения существующих аналогов до детального проектирования архитектуры, непосредственной реализации и комплексного тестирования системы.
     Особое внимание в ходе разработки было уделено созданию интуитивно понятного пользовательского интерфейса, что обеспечивает быстрое освоение системы сотрудниками без специальной технической подготовки и минимизирует время адаптации персонала. Реализованное приложение позволяет существенно повысить эффективность управления потоками пациентов, свести к минимуму ошибки при записи на приём и оптимизировать все ключевые административные процессы медицинского учреждения. Встроенная система напоминаний о предстоящих визитах способствует снижению частоты неявок пациентов и повышению дисциплины лечения, а персонализированные рабочие места врача и администратора ускоряют обработку медицинской документации и формирование отчётности.
     Перспективные направления развития системы включают:
     1 Интеграцию с системами электронного документооборота и медицинскими информационными системами (МИС);
     2 Разработку мобильного интерфейса для пациентов с push-уведомлениями и доступом через смартфоны;
     3 Внедрение модулей прогнозной аналитики на основе методов машинного обучения для оптимизации расписания врачей;
     4 Адаптацию системы для различных типов медицинских учреждений (поликлиники, стоматологии, диагностические центры);
     5 Реализацию функций управления распределёнными филиалами и единой базой пациентов.
     Успешная реализация проекта наглядно подтвердила возможность создания эффективных цифровых инструментов для автоматизации работы регистратуры и ведения электронных медицинских карт с использованием современных технологий разработки программного обеспечения. Внедрение подобных решений позволяет медицинским организациям не только оптимизировать текущие бизнес-процессы, но и получить значительное конкурентное преимущество на динамично развивающемся рынке медицинских услуг.
СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ
     
     [1] Electronic Health Record (EHR) Standards [Электронный ресурс]. – Режим доступа: https://www.hl7.org/.
     [2] 1С:Медицина [Электронный ресурс]. – Режим доступа: https://solutions.1c.ru/catalog/medicine.
     [3] YCLIENTS [Электронный ресурс]. – Режим доступа: https://yclients.com/.
     [4] MedExpert [Электронный ресурс]. – Режим доступа: https://medexpert.ru/.
     [5] Google Calendar [Электронный ресурс]. – Режим доступа: https://calendar.google.com/.
     [6] IDEF0 Standard [Электронный ресурс]. – Режим доступа: https://www.idef.com/idef0-fuction-modeling-method/.
     [7] Unified Modeling Language (UML) Specification Version 2.5.1 [Электронный ресурс]. – Режим доступа: https://www.omg.org/spec/UML/2.5.1.
     [8] TCP/IP Sockets in Java: Practical Guide for Programmers [Электронный ресурс]. – Режим доступа: https://www.elsevier.com/books/tcpip-sockets-in-java/calvert/978-0-12-374255-1.
     [9] JSON (JavaScript Object Notation) [Электронный ресурс]. – Режим доступа: https://www.json.org/json-ru.html.
     [10] Oracle Java Concurrency [Электронный ресурс]. – Режим доступа: https://docs.oracle.com/javase/tutorial/essential/concurrency/
     [11] Java [Электронный ресурс]. – Режим доступа: https://www.java.com/ru/.
     [12] JavaFX [Электронный ресурс]. – Режим доступа: https://openjfx.io/.
     [13] PostgreSQL [Электронный ресурс]. – Режим доступа: https://www.postgresql.org/docs/.
     [14] ACID Properties in Database Management Systems [Электронный ресурс]. – Режим доступа: https://www.postgresql.org/docs/current/mvcc.html.
     [15] IntelliJ IDEA [Электронный ресурс]. – Режим доступа: https://www.jetbrains.com/idea/.
     [16] Apache Maven [Электронный ресурс]. – Режим доступа: https://maven.apache.org/.
     [17] JavaFX FXML [Электронный ресурс]. – Режим доступа: https://openjfx.io/javadoc/21/javafx.fxml/javafx/fxml/doc-files/introduction_to_fxml.html.
     [18] SQL (Structured Query Language) [Электронный ресурс]. – Режим доступа: https://www.postgresql.org/docs/current/sql.html
     [19] FIPS 180-4: Secure Hash Standard (SHA-256) [Электронный ресурс]. – Режим доступа: https://csrc.nist.gov/pubs/fips/180-4/upd/final.
     [20] Password Hashing with Salt [Электронный ресурс]. – Режим доступа: https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html.
     [21] OWASP SQL Injection Prevention Cheat Sheet [Электронный ресурс]. – Режим доступа: https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html.
     [22] Software Testing Fundamentals [Электронный ресурс]. – Режим доступа: https://www.istqb.org/.
     [23] CSV (Comma-Separated Values) Format [Электронный ресурс]. – Режим доступа: https://www.rfc-editor.org/rfc/rfc4180.
     [24] JDBC PostgreSQL Driver [Электронный ресурс]. – Режим доступа: https://jdbc.postgresql.org/.
     [25] Java Socket Programming [Электронный ресурс]. – Режим доступа: https://docs.oracle.com/javase/tutorial/networking/sockets/.


Приложение А
(обязательное)
Справка о проверке курсового проекта на плагиат
     
     
     Рисунок А.1 – Отчет о проверке на заимствования в системе «Антиплагиат»


Приложение Б
(обязательное)
     Листинг кода алгоритмов, реализующих основную бизнес-логику приложения
     
import java.net.*;
import java.util.logging.Logger;

public class Main {

    private static final Logger LOG = Logger.getLogger(Main.class.getName());

    public static void main(String[] args) {
        try (ServerSocket serverSocket = new ServerSocket(5555)) {
            LOG.info("Сервер запущен на порту 5555");

            DataBase.DBHelper.getInstance();

            while (true) {
                Socket clientSocket = serverSocket.accept();
                LOG.info("Новый клиент: " + clientSocket.getInetAddress().getHostAddress());
                new Thread(new ClientThread(clientSocket)).start();
            }
        } catch (Exception e) {
            LOG.severe("Ошибка сервера: " + e.getMessage());
            e.printStackTrace();
        }
    }
}

import org.example.client.common.Command;
import org.example.client.common.CommandStatus;
import DAO.*;
import models.*;

import java.util.List;
import java.util.logging.Logger;

public class CommandHandler {

    private static final Logger LOG = Logger.getLogger(CommandHandler.class.getName());

    private final UserDAO userDAO = new UserDAO();
    private final PatientDAO patientDAO = new PatientDAO();
    private final DoctorDAO doctorDAO = new DoctorDAO();
    private final AppointmentDAO appointmentDAO = new AppointmentDAO();
    private final ScheduleSlotDAO slotDAO = new ScheduleSlotDAO();
    private final ReminderDAO reminderDAO = new ReminderDAO();
    private final AuditLogDAO auditLogDAO = new AuditLogDAO();
    private final VisitDetailsDAO visitDetailsDAO = new VisitDetailsDAO();

    public Command handle(Command cmd, long userId, Role role, String ip) {
        try {
            return switch (cmd.getType()) {
                case LOGIN           -> handleLogin(cmd, ip);
                case REGISTER        -> handleRegister(cmd, ip);
                case LOGOUT          -> handleLogout(cmd);
                case CHANGE_PASSWORD -> handleChangePassword(cmd, userId);
                case GET_PROFILE     -> handleGetProfile(cmd, userId);
Продолжение приложения Б

                case GET_ALL_DOCTORS    -> handleGetAllDoctors(cmd);
                case GET_DOCTORS_BY_SPEC-> handleGetDoctorsBySpec(cmd);
                case GET_DOCTOR_BY_ID   -> handleGetDoctorById(cmd);
                case GET_SPECIALIZATIONS-> handleGetSpecializations(cmd);
                case GET_SLOTS         -> handleGetSlots(cmd);
                case GET_DOCTOR_SCHEDULE-> handleGetDoctorSchedule(cmd);
                case BLOCK_SLOT        -> handleBlockSlot(cmd, true);
                case UNBLOCK_SLOT      -> handleBlockSlot(cmd, false);
                case BOOK_APPOINTMENT      -> handleBookAppointment(cmd, userId, role);
                case CANCEL_APPOINTMENT    -> handleCancelAppointment(cmd, userId);
                case RESCHEDULE_APPOINTMENT-> handleRescheduleAppointment(cmd);
                case CONFIRM_APPOINTMENT   -> handleStatusChange(cmd, AppointmentStatus.CONFIRMED);
                case MARK_COMPLETED        -> handleStatusChange(cmd, AppointmentStatus.COMPLETED);
                case MARK_MISSED           -> handleStatusChange(cmd, AppointmentStatus.MISSED);
                case GET_MY_APPOINTMENTS   -> handleGetMyAppointments(cmd, userId, role);
                case GET_ALL_APPOINTMENTS  -> handleGetAllAppointments(cmd);
                case GET_APPOINTMENT_BY_ID -> handleGetAppointmentById(cmd);
                case GET_HISTORY           -> handleGetHistory(cmd, userId, role);
                case GET_APPOINTMENT_DETAILS-> handleGetAppointmentDetails(cmd);
                case SAVE_DIAGNOSIS        -> handleSaveDiagnosis(cmd, userId);
                case GET_REMINDERS -> handleGetReminders(cmd, userId, role);
                case CREATE_REMINDER       -> handleCreateReminder(cmd);
                case UPDATE_REMINDER       -> handleUpdateReminder(cmd);
                case CANCEL_REMINDER       -> handleCancelReminder(cmd);
                case TOGGLE_REMINDER       -> handleToggleReminder(cmd);
                case GET_PATIENT_BY_ID     -> handleGetPatientById(cmd);
                case SEARCH_PATIENTS       -> handleSearchPatients(cmd);
                case UPDATE_PATIENT        -> handleUpdatePatient(cmd);
                case GET_ALL_USERS -> handleGetAllUsers(cmd, role);
                case CREATE_USER   -> handleCreateUser(cmd, role);
                case BLOCK_USER    -> handleBlockUser(cmd, true,  role, userId);
                case UNBLOCK_USER  -> handleBlockUser(cmd, false, role, userId);
                case DELETE_USER   -> handleDeleteUser(cmd, role);
                case ADD_DOCTOR    -> handleAddDoctor(cmd, role);
                case UPDATE_DOCTOR -> handleUpdateDoctor(cmd, role);
                case REMOVE_DOCTOR -> handleRemoveDoctor(cmd, role);
                case GET_AUDIT_LOG -> handleGetAuditLog(cmd, role);
                case PING          -> ok(cmd).message("pong").build();
                default            -> error(cmd, "Неизвестная команда: " + cmd.getType());
            };
        } catch (Exception ex) {
            LOG.severe("[Handler] Необработанная ошибка: " + ex.getMessage());
            return error(cmd, "Внутренняя ошибка сервера: " + ex.getMessage());
        }
    }

Продолжение приложения Б


    private Command handleLogin(Command cmd, String ip) {
        String login = cmd.getParam("login");
        String passHash = cmd.getParam("password");
        if (login == null || passHash == null)
            return clientError(cmd, "Не переданы логин или пароль.");

        User user = userDAO.findByLogin(login);
        if (user == null || !user.getPasswordHash().equals(passHash))
            return clientError(cmd, "Неверный логин или пароль.");
        if (user.isBlocked())
            return clientError(cmd, "Учётная запись заблокирована.");

        String token = userDAO.createSession(user.getId(), ip);
        if (token == null)
            return error(cmd, "Не удалось создать сессию.");

        // Получаем полное имя пользователя
        String fullName = login; // fallback — email
        if (user.getRole() == Role.PATIENT) {
            Patient patient = patientDAO.findByUserId(user.getId());
            if (patient != null) fullName = patient.getFullName();
        } else if (user.getRole() == Role.DOCTOR) {
            Doctor doctor = doctorDAO.findByUserId(user.getId());
            if (doctor != null) {
                fullName = doctor.getFullName();
            }
        }

        long doctorId = 0;
        if (user.getRole() == Role.DOCTOR) {
            Doctor doctor = doctorDAO.findByUserId(user.getId());
            if (doctor != null) {
                fullName = doctor.getFullName();
                doctorId = doctor.getId();
            }
        }

        audit(user.getId(), login, "LOGIN", "User",
                String.valueOf(user.getId()), "Успешный вход с " + ip, "INFO");

        String specialization = "";
        if (user.getRole() == Role.DOCTOR) {
            Doctor doctor = doctorDAO.findByUserId(user.getId());
            if (doctor != null && doctor.getSpecialization() != null) {
                specialization = doctor.getSpecialization().getName();
            }
        }

        return ok(cmd)
                .param("token", token)
                .param("userId", user.getId())
                .param("role", user.getRole().name())
                .param("fullName", fullName)
                .param("doctorId", doctorId)
                .param("specialization", specialization)
                .build();
    }

    private Command handleRegister(Command cmd, String ip) {
Продолжение приложения Б

        String fullName  = cmd.getParam("fullName");
        String email     = cmd.getParam("email");
        String passHash  = cmd.getParam("password");

        if (email == null || passHash == null || fullName == null)
            return clientError(cmd, "Заполните все обязательные поля.");

        if (userDAO.findByLogin(email) != null)
            return clientError(cmd, "Email уже зарегистрирован.");

        long userId = userDAO.create(email, passHash, "", Role.PATIENT);
        if (userId < 0)
            return error(cmd, "Не удалось создать пользователя.");

        String phone = cmd.getParam("phone", "");
        patientDAO.create(userId, fullName, phone, email);

        String token = userDAO.createSession(userId, ip);

        audit(userId, email, "REGISTER", "User", String.valueOf(userId),
                "Регистрация: " + fullName, "INFO");

        return ok(cmd)
                .param("token",  token != null ? token : "")
                .param("userId", userId)
                .param("role",   "PATIENT")
                .build();
    }

    private Command handleLogout(Command cmd) {
        String token = cmd.getSessionToken();
        if (token != null) userDAO.deleteSession(token);
        return ok(cmd).message("Сессия завершена.").build();
    }

    private Command handleChangePassword(Command cmd, long userId) {
        String newHash = cmd.getParam("newPassword");
        if (newHash == null) return clientError(cmd, "Новый пароль не указан.");
        boolean ok = userDAO.changePassword(userId, newHash);
        return ok ? ok(cmd).message("Пароль изменён.").build()
                : error(cmd, "Не удалось изменить пароль.");
    }

    private Command handleGetProfile(Command cmd, long userId) {
        User user = userDAO.findById(userId);
        if (user == null) return clientError(cmd, "Пользователь не найден.");

        Command.Builder b = ok(cmd)
                .param("login", user.getLogin())
                .param("role", user.getRole().name());

        if (user.getRole() == Role.PATIENT) {
            Patient p = patientDAO.findByUserId(userId);
            if (p != null) {
                b.param("fullName", p.getFullName())
                        .param("phone", p.getPhone() != null ? p.getPhone() : "")
                        .param("email", p.getEmail() != null ? p.getEmail() : "")
                        .param("policy", p.getPolicy() != null ? 
Продолжение приложения Б

p.getPolicy() : "")
                        .param("birthDate", p.getDateOfBirth() != null ? p.getDateOfBirth().toString() : "")
                        .param("allergy", p.getAllergy() != null ? p.getAllergy() : "")
                        .param("bloodType", p.getBloodType() != null ? p.getBloodType() : "");
            }
        }

        if (user.getRole() == Role.DOCTOR) {
            Doctor doctor = doctorDAO.findByUserId(userId);
            if (doctor != null) {
                b.param("doctorId", doctor.getId())
                        .param("fullName", doctor.getFullName())
                        .param("specialization", doctor.getSpecialization() != null ? doctor.getSpecialization().getName() : "");
            }
        }

        return b.build();
    }

    private Command handleGetAllDoctors(Command cmd) {
        List<Doctor> doctors = doctorDAO.findAll();
        return serializeDoctors(cmd, doctors);
    }

    private Command handleGetDoctorsBySpec(Command cmd) {
        int specId = cmd.getIntParam("specializationId");
        List<Doctor> doctors = specId > 0
                ? doctorDAO.findBySpecialization(specId)
                : doctorDAO.findAll();
        return serializeDoctors(cmd, doctors);
    }

    private Command handleGetDoctorById(Command cmd) {
        long id = Long.parseLong(cmd.getParam("doctorId", "0"));
        Doctor d = doctorDAO.findById(id);
        if (d == null) return clientError(cmd, "Врач не найден.");
        return ok(cmd).param("json", serializeDoctor(d)).build();
    }

    private Command handleGetSpecializations(Command cmd) {
        List<Specialization> specs = doctorDAO.findAllSpecializations();
        StringBuilder sb = new StringBuilder();
        for (Specialization s : specs) {
            if (sb.length() > 0) sb.append("|");
            sb.append(s.getId()).append(":").append(s.getName());
        }
        return ok(cmd).param("specializations", sb.toString()).build();
    }

    private Command handleGetSlots(Command cmd) {
        long   doctorId = Long.parseLong(cmd.getParam("doctorId", "0"));
        String date     = cmd.getParam("date");
        System.out.println("GET_SLOTS doctorId=" + doctorId + " date=" + date);
        if (doctorId == 0 || date == null) return clientError(cmd, "Не указан врач или дата.");

Продолжение приложения Б


        List<ScheduleSlot> slots = slotDAO.findFree(doctorId, date);
        StringBuilder sb = new StringBuilder();
        for (ScheduleSlot s : slots) {
            if (sb.length() > 0) sb.append("|");
            sb.append(s.getId()).append(":").append(s.getStartTime());
        }
        return ok(cmd).param("slots", sb.toString()).build();
    }

    private Command handleGetDoctorSchedule(Command cmd) {
        long doctorId = Long.parseLong(cmd.getParam("doctorId", "0"));
        String from = cmd.getParam("from");
        String to = cmd.getParam("to");
        List<ScheduleSlot> slots = slotDAO.findByDoctorAndPeriod(doctorId, from, to);

        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < slots.size(); i++) {
            if (i > 0) sb.append(",");
            ScheduleSlot s = slots.get(i);
            sb.append(String.format(
                    "{\"id\":\"%d\",\"date\":\"%s\",\"start\":\"%s\",\"end\":\"%s\"," +
                            "\"booked\":\"%s\",\"blocked\":\"%s\"}",
                    s.getId(), s.getSlotDate(), s.getStartTime(), s.getEndTime(),
                    s.isBooked(), s.isBlocked()));
        }
        sb.append("]");
        return ok(cmd).param("slots", sb.toString()).build();
    }

    private Command handleBlockSlot(Command cmd, boolean block) {
        long slotId = Long.parseLong(cmd.getParam("slotId", "0"));
        boolean ok  = block ? slotDAO.setBlocked(slotId, true) : slotDAO.setBlocked(slotId, false);
        return ok ? ok(cmd).message(block ? "Слот заблокирован." : "Слот разблокирован.").build()
                : error(cmd, "Не удалось изменить статус слота.");
    }

    private Command handleBookAppointment(Command cmd, long userId, Role role) {
        long   doctorId = Long.parseLong(cmd.getParam("doctorId", "0"));
        long   slotId   = Long.parseLong(cmd.getParam("slotId",   "0"));
        String reason   = cmd.getParam("reason", "");
        String dtStr    = cmd.getParam("datetime");

        if (doctorId == 0 || slotId == 0 || dtStr == null)
            return clientError(cmd, "Не указаны обязательные параметры.");

        long patientId;
        if (role == Role.PATIENT) {
            Patient p = patientDAO.findByUserId(userId);
            if (p == null) return clientError(cmd, "Профиль пациента не найден.");
            patientId = p.getId();
        } else {
            patientId = Long.parseLong(cmd.getParam("patientId", "0"));
        }
Продолжение приложения Б


        java.time.LocalDateTime dt = java.time.LocalDateTime.parse(dtStr);
        long apptId = appointmentDAO.create(patientId, doctorId, slotId, dt, reason);
        if (apptId < 0) return error(cmd, "Не удалось создать запись.");

        slotDAO.setBooked(slotId, true);

        reminderDAO.create(apptId, dt.minusHours(24), 1440, "EMAIL");
        reminderDAO.create(apptId, dt.minusHours(2),   120, "EMAIL");

        audit(userId, null, "BOOK_APPOINTMENT", "Appointment", String.valueOf(apptId),
                "Запись #" + apptId + " к врачу #" + doctorId, "INFO");

        return ok(cmd).param("appointmentId", apptId).build();
    }

    private Command handleCancelAppointment(Command cmd, long userId) {
        long apptId = Long.parseLong(cmd.getParam("appointmentId", "0"));
        if (apptId == 0) return clientError(cmd, "Не указан id записи.");

        Appointment appt = appointmentDAO.findById(apptId);
        if (appt == null) return clientError(cmd, "Запись не найдена.");

        boolean ok = appointmentDAO.updateStatus(apptId, AppointmentStatus.CANCELLED);
        if (ok) {
            reminderDAO.cancelByAppointment(apptId);
            // освобождаем слот
            if (appt.getSlotId() > 0) {
                slotDAO.setBooked(appt.getSlotId(), false);
            }
            audit(userId, null, "CANCEL_APPOINTMENT", "Appointment",
                    String.valueOf(apptId), "Отмена записи #" + apptId, "WARN");
        }
        return ok ? ok(cmd).message("Запись отменена.").build()
                : error(cmd, "Не удалось отменить запись.");
    }

    private Command handleRescheduleAppointment(Command cmd) {
        long   apptId   = Long.parseLong(cmd.getParam("appointmentId", "0"));
        long   newSlotId= Long.parseLong(cmd.getParam("newSlotId",    "0"));
        String dtStr    = cmd.getParam("datetime");
        if (apptId == 0 || newSlotId == 0 || dtStr == null)
            return clientError(cmd, "Не указаны параметры переноса.");

        java.time.LocalDateTime newDt = java.time.LocalDateTime.parse(dtStr);
        boolean ok = appointmentDAO.reschedule(apptId, newSlotId, newDt);
        return ok ? ok(cmd).message("Запись перенесена.").build()
                : error(cmd, "Не удалось перенести запись.");
    }

    private Command handleStatusChange(Command cmd, AppointmentStatus newStatus) {
        long apptId = Long.parseLong(cmd.getParam("appointmentId", "0"));
        if (apptId == 0) return clientError(cmd, "Не указан id записи.");
        boolean ok = appointmentDAO.updateStatus(apptId, newStatus);
        return ok ? ok(cmd).message("Статус обновлён: " + newStatus).build()
                : error(cmd, "Не удалось обновить статус.");
Продолжение приложения Б

    }

    private Command handleGetMyAppointments(Command cmd, long userId, Role role) {
        String status = cmd.getParam("status");
        List<Appointment> list;
        if (role == Role.PATIENT) {
            Patient p = patientDAO.findByUserId(userId);
            if (p == null) return ok(cmd).param("json", "[]").build();
            list = appointmentDAO.findByPatient(p.getId(), status);
        } else {
            list = appointmentDAO.findAll(null, null);
        }
        return ok(cmd).param("json", serializeAppointments(list)).build();
    }

    private Command handleGetAllAppointments(Command cmd) {
        String doctorIdStr = cmd.getParam("doctorId");
        Long   doctorId    = doctorIdStr != null ? Long.parseLong(doctorIdStr) : null;
        String date        = cmd.getParam("date");
        List<Appointment> list = appointmentDAO.findAll(doctorId, date);
        return ok(cmd).param("json", serializeAppointments(list)).build();
    }

    private Command handleGetAppointmentById(Command cmd) {
        long id = Long.parseLong(cmd.getParam("appointmentId", "0"));
        Appointment a = appointmentDAO.findById(id);
        if (a == null) return clientError(cmd, "Запись не найдена.");
        return ok(cmd).param("json", serializeAppointment(a)).build();
    }

    private Command handleGetHistory(Command cmd, long userId, Role role) {
        long patientId;
        if (role == Role.PATIENT) {
            Patient p = patientDAO.findByUserId(userId);
            if (p == null) return ok(cmd).param("json", "[]").build();
            patientId = p.getId();
        } else {
            patientId = Long.parseLong(cmd.getParam("patientId", "0"));
        }
        List<Appointment> list = appointmentDAO.findHistory(patientId);
        return ok(cmd).param("json", serializeAppointments(list)).build();
    }

    private Command handleGetAppointmentDetails(Command cmd) {
        long apptId = Long.parseLong(cmd.getParam("appointmentId", "0"));
        VisitDetails v = visitDetailsDAO.findByAppointmentId(apptId);
        if (v == null) return clientError(cmd, "Детали визита не найдены.");
        return ok(cmd).param("json", serializeVisitDetails(v)).build();
    }

    private Command handleSaveDiagnosis(Command cmd, long userId) {
        long apptId = Long.parseLong(cmd.getParam("appointmentId", "0"));
        VisitDetails v = new VisitDetails();
        v.setAppointmentId(apptId);
        v.setComplaints(cmd.getParam("complaints", ""));
        v.setExamination(cmd.getParam("examination", ""));
        v.setDiagnosisCode(cmd.getParam("diagnosisCode", ""));
        v.setDiagnosisText(cmd.getParam("diagnosisText", ""));
        v.setPrescription(cmd.getParam("prescription", ""));
Продолжение приложения Б

        v.setNotes(cmd.getParam("notes", ""));

        long id = visitDetailsDAO.save(v);
        if (id < 0) return error(cmd, "Не удалось сохранить диагноз.");

        boolean statusUpdated = appointmentDAO.updateStatus(apptId, AppointmentStatus.COMPLETED);
        if (!statusUpdated) return error(cmd, "Не удалось обновить статус записи.");

        Appointment appt = appointmentDAO.findById(apptId);
        if (appt != null && appt.getSlotId() > 0) {
            slotDAO.setBooked(appt.getSlotId(), false);
        }

        reminderDAO.cancelByAppointment(apptId);

        audit(userId, null, "SAVE_DIAGNOSIS", "Visit", String.valueOf(apptId),
                "Диагноз сохранён: " + v.getDiagnosisCode() + ". Приём завершён.", "INFO");

        return ok(cmd).param("visitDetailsId", id).build();
    }

    private Command handleGetReminders(Command cmd, long userId, Role role) {
        String status = cmd.getParam("status");
        List<Reminder> list;
        if (role == Role.ADMIN) {
            list = reminderDAO.findAll();
        } else {
            list = reminderDAO.findByPatientUserId(userId, status);
        }
        return ok(cmd).param("json", serializeReminders(list)).build();
    }

    private Command handleCreateReminder(Command cmd) {
        long   apptId  = Long.parseLong(cmd.getParam("appointmentId", "0"));
        int    minutes = cmd.getIntParam("intervalMinutes");
        String channel = cmd.getParam("channel", "EMAIL");
        String dtStr   = cmd.getParam("sendTime");

        java.time.LocalDateTime sendTime = dtStr != null
                ? java.time.LocalDateTime.parse(dtStr)
                : java.time.LocalDateTime.now().plusMinutes(minutes);

        long id = reminderDAO.create(apptId, sendTime, minutes, channel);
        return id > 0 ? ok(cmd).param("reminderId", id).build()
                : error(cmd, "Не удалось создать напоминание.");
    }

    private Command handleUpdateReminder(Command cmd) {
        long   id      = Long.parseLong(cmd.getParam("reminderId", "0"));
        int    minutes = cmd.getIntParam("intervalMinutes");
        String channel = cmd.getParam("channel", "EMAIL");
        return reminderDAO.update(id, minutes, channel)
                ? ok(cmd).message("Напоминание обновлено.").build()
                : error(cmd, "Не удалось обновить напоминание.");
    }

    private Command handleCancelReminder(Command cmd) {
Продолжение приложения Б

        long id = Long.parseLong(cmd.getParam("reminderId", "0"));
        return reminderDAO.cancel(id)
                ? ok(cmd).message("Напоминание отменено.").build()
                : error(cmd, "Не удалось отменить напоминание.");
    }

    private Command handleToggleReminder(Command cmd) {
        long    id      = Long.parseLong(cmd.getParam("reminderId", "0"));
        boolean enabled = Boolean.parseBoolean(cmd.getParam("enabled", "true"));
        return reminderDAO.toggle(id, enabled)
                ? ok(cmd).message("Статус напоминания изменён.").build()
                : error(cmd, "Не удалось изменить статус напоминания.");
    }

    private Command handleGetPatientById(Command cmd) {
        long id = Long.parseLong(cmd.getParam("patientId", "0"));
        Patient p = patientDAO.findById(id);
        if (p == null) return clientError(cmd, "Пациент не найден.");
        return ok(cmd).param("json", serializePatient(p)).build();
    }

    private Command handleSearchPatients(Command cmd) {
        String query = cmd.getParam("query", "");
        List<Patient> list = patientDAO.search(query);
        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < list.size(); i++) {
            if (i > 0) sb.append(",");
            sb.append(serializePatient(list.get(i)));
        }
        sb.append("]");
        return ok(cmd).param("json", sb.toString()).build();
    }

    private Command handleUpdatePatient(Command cmd) {
        long id = Long.parseLong(cmd.getParam("patientId", "0"));
        Patient p = patientDAO.findById(id);
        if (p == null) return clientError(cmd, "Пациент не найден.");
        if (cmd.hasParam("fullName"))  p.setFullName(cmd.getParam("fullName"));
        if (cmd.hasParam("phone"))     p.setPhone(cmd.getParam("phone"));
        if (cmd.hasParam("email"))     p.setEmail(cmd.getParam("email"));
        if (cmd.hasParam("policy"))    p.setPolicy(cmd.getParam("policy"));
        if (cmd.hasParam("allergy"))   p.setAllergy(cmd.getParam("allergy"));
        if (cmd.hasParam("bloodType")) p.setBloodType(cmd.getParam("bloodType"));
        return patientDAO.update(p)
                ? ok(cmd).message("Данные пациента обновлены.").build()
                : error(cmd, "Не удалось обновить данные пациента.");
    }

    private Command handleGetAllUsers(Command cmd, Role role) {
        if (role != Role.ADMIN) return Command.unauthorized(cmd);
        List<User> users = userDAO.findAll();
        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < users.size(); i++) {
            if (i > 0) sb.append(",");
            User u = users.get(i);
            sb.append(String.format(
                    "{\"id\":\"%d\",\"login\":\"%s\",\"role\":\"%s\",\"blocked\":\"%s\"}",
Продолжение приложения Б

                    u.getId(), u.getLogin(), u.getRole(), u.isBlocked()));  
        }
        sb.append("]");
        return ok(cmd).param("json", sb.toString()).build();
    }

    private Command handleCreateUser(Command cmd, Role role) {
        if (role != Role.ADMIN) return Command.unauthorized(cmd);
        String login    = cmd.getParam("login");
        String passHash = cmd.getParam("password");
        String roleStr  = cmd.getParam("role", "PATIENT");
        if (login == null || passHash == null) return clientError(cmd, "Логин и пароль обязательны.");
        if (userDAO.findByLogin(login) != null) return clientError(cmd, "Логин уже занят.");
        long id = userDAO.create(login, passHash, "", Role.valueOf(roleStr));
        return id > 0 ? ok(cmd).param("userId", id).build()
                : error(cmd, "Не удалось создать пользователя.");
    }

    private Command handleBlockUser(Command cmd, boolean block, Role role, long callerId) {
        if (role != Role.ADMIN) return Command.unauthorized(cmd);
        long userId = Long.parseLong(cmd.getParam("userId", "0"));
        if (userId == callerId) return clientError(cmd, "Нельзя заблокировать самого себя.");
        boolean ok = userDAO.setBlocked(userId, block);
        if (ok) audit(callerId, null, block ? "BLOCK_USER" : "UNBLOCK_USER",
                "User", String.valueOf(userId),
                (block ? "Блокировка" : "Разблокировка") + " userId=" + userId, "WARN");
        return ok ? ok(cmd).message(block ? "Пользователь заблокирован." : "Разблокирован.").build()
                : error(cmd, "Операция не выполнена.");
    }

    private Command handleDeleteUser(Command cmd, Role role) {
        if (role != Role.ADMIN) return Command.unauthorized(cmd);
        long userId = Long.parseLong(cmd.getParam("userId", "0"));
        return userDAO.delete(userId)
                ? ok(cmd).message("Пользователь удалён.").build()
                : error(cmd, "Не удалось удалить пользователя.");
    }

    private Command handleAddDoctor(Command cmd, Role role) {
        if (role != Role.ADMIN) return Command.unauthorized(cmd);
        Doctor d = new Doctor();
        d.setFullName(cmd.getParam("fullName", ""));
        int specId = cmd.getIntParam("specializationId");
        if (specId > 0) d.setSpecialization(new Specialization(specId, ""));
        d.setOfficeNumber(cmd.getParam("officeNumber", ""));
        d.setEducation(cmd.getParam("education", ""));
        d.setAbout(cmd.getParam("about", ""));
        d.setExperienceYears(cmd.getIntParam("experienceYears"));
        d.setActive(true);
        long id = doctorDAO.create(d);
        return id > 0 ? ok(cmd).param("doctorId", id).build()
                : error(cmd, "Не удалось добавить врача.");
    }

    private Command handleUpdateDoctor(Command cmd, Role role) {
Продолжение приложения Б

        if (role != Role.ADMIN) return Command.unauthorized(cmd);
        long id = Long.parseLong(cmd.getParam("doctorId", "0"));
        Doctor d = doctorDAO.findById(id);
        if (d == null) return clientError(cmd, "Врач не найден.");
        if (cmd.hasParam("fullName"))      d.setFullName(cmd.getParam("fullName"));
        if (cmd.hasParam("officeNumber"))  d.setOfficeNumber(cmd.getParam("officeNumber"));
        if (cmd.hasParam("education"))     d.setEducation(cmd.getParam("education"));
        if (cmd.hasParam("active"))        d.setActive(Boolean.parseBoolean(cmd.getParam("active")));
        return doctorDAO.update(d)
                ? ok(cmd).message("Данные врача обновлены.").build()
                : error(cmd, "Не удалось обновить врача.");
    }

    private Command handleRemoveDoctor(Command cmd, Role role) {
        if (role != Role.ADMIN) return Command.unauthorized(cmd);
        long id = Long.parseLong(cmd.getParam("doctorId", "0"));
        return doctorDAO.delete(id)
                ? ok(cmd).message("Врач удалён.").build()
                : error(cmd, "Не удалось удалить врача.");
    }

    private Command handleGetAuditLog(Command cmd, Role role) {
        if (role != Role.ADMIN) return Command.unauthorized(cmd);
        String from       = cmd.getParam("from");
        String to         = cmd.getParam("to");
        String userIdStr  = cmd.getParam("userId");
        Long   userId     = userIdStr != null ? Long.parseLong(userIdStr) : null;
        List<AuditLog> logs = auditLogDAO.findAll(from, to, userId);
        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < logs.size(); i++) {
            if (i > 0) sb.append(",");
            AuditLog l = logs.get(i);
            sb.append(String.format(
                    "{\"id\":\"%d\",\"userLogin\":\"%s\",\"action\":\"%s\"," +
                            "\"entityType\":\"%s\",\"details\":\"%s\",\"level\":\"%s\",\"timestamp\":\"%s\"}",
                    l.getId(), safe(l.getUserLogin()), safe(l.getAction()),
                    safe(l.getEntityType()), safe(l.getDetails()), safe(l.getLevel()),
                    l.getTimestamp()));
        }
        sb.append("]");
        return ok(cmd).param("json", sb.toString()).build();
    }

    private Command serializeDoctors(Command cmd, List<Doctor> doctors) {
        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < doctors.size(); i++) {
            if (i > 0) sb.append(",");
            sb.append(serializeDoctor(doctors.get(i)));
        }
        sb.append("]");
        return ok(cmd).param("json", sb.toString()).build();
    }
Продолжение приложения Б


    private String serializeDoctor(Doctor d) {
        return String.format(
                "{\"id\":\"%d\",\"fullName\":\"%s\",\"specialization\":\"%s\",\"specId\":\"%d\"," +
                        "\"officeNumber\":\"%s\",\"education\":\"%s\",\"about\":\"%s\"," +
                        "\"experienceYears\":\"%d\",\"active\":\"%s\",\"price\":\"%d\"}",
                d.getId(),
                safe(d.getFullName()),
                d.getSpecialization() != null ? d.getSpecialization().getName() : "",
                d.getSpecialization() != null ? d.getSpecialization().getId()   : 0,
                safe(d.getOfficeNumber()),
                safe(d.getEducation()),
                safe(d.getAbout()),
                d.getExperienceYears(),
                d.isActive(),                       // "true"/"false"
                d.getPrice()
        );
    }

    private String serializeAppointments(List<Appointment> list) {
        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < list.size(); i++) {
            if (i > 0) sb.append(",");
            sb.append(serializeAppointment(list.get(i)));
        }
        return sb.append("]").toString();
    }

    private String serializeAppointment(Appointment a) {
        return String.format(
                "{\"id\":\"%d\",\"patientId\":\"%d\",\"doctorId\":\"%d\",\"slotId\":\"%d\"," +
                        "\"datetime\":\"%s\",\"status\":\"%s\",\"reason\":\"%s\"," +
                        "\"patientName\":\"%s\",\"doctorName\":\"%s\",\"specialization\":\"%s\"," +
                        "\"diagnosis\":\"%s\",\"prescription\":\"%s\"}",
                a.getId(), a.getPatientId(), a.getDoctorId(), a.getSlotId(),
                a.getAppointmentDatetime(), a.getStatus().name(),
                safe(a.getReason()), safe(a.getPatientName()),
                safe(a.getDoctorName()), safe(a.getSpecialization()),
                safe(a.getDiagnosis()), safe(a.getPrescription()));
    }

    private String serializeReminders(List<Reminder> list) {
        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < list.size(); i++) {
            if (i > 0) sb.append(",");
            Reminder r = list.get(i);
            sb.append(String.format(
                    "{\"id\":\"%d\",\"appointmentId\":\"%d\",\"scheduledSendTime\":\"%s\"," +
                            "\"intervalMinutes\":\"%d\",\"channel\":\"%s\",\"status\":\"%s\"," +
Продолжение приложения Б

                            "\"enabled\":\"%s\",\"patientName\":\"%s\",\"doctorName\":\"%s\"," +
                            "\"specialization\":\"%s\",\"appointmentDatetime\":\"%s\"}",
                    r.getId(), r.getAppointmentId(), r.getScheduledSendTime(),
                    r.getIntervalMinutes(), r.getChannel(), r.getStatus().name(),
                    r.isEnabled(),                     // "true"/"false"
                    safe(r.getPatientName()), safe(r.getDoctorName()),
                    safe(r.getSpecialization()), r.getAppointmentDatetime()));
        }
        return sb.append("]").toString();
    }

    private String serializePatient(Patient p) {
        return String.format(
                "{\"id\":\"%d\",\"userId\":\"%d\",\"fullName\":\"%s\",\"phone\":\"%s\"," +
                        "\"email\":\"%s\",\"dateOfBirth\":\"%s\",\"policy\":\"%s\"," +
                        "\"allergy\":\"%s\",\"bloodType\":\"%s\"}",
                p.getId(), p.getUserId(), safe(p.getFullName()), safe(p.getPhone()),
                safe(p.getEmail()), p.getDateOfBirth() != null ? p.getDateOfBirth() : "",
                safe(p.getPolicy()), safe(p.getAllergy()), safe(p.getBloodType()));
    }

    private String serializeVisitDetails(VisitDetails v) {
        return String.format(
                "{\"id\":\"%d\",\"appointmentId\":\"%d\",\"complaints\":\"%s\"," +
                        "\"examination\":\"%s\",\"diagnosisCode\":\"%s\",\"diagnosisText\":\"%s\"," +
                        "\"prescription\":\"%s\",\"notes\":\"%s\"}",
                v.getId(), v.getAppointmentId(),
                safe(v.getComplaints()), safe(v.getExamination()),
                safe(v.getDiagnosisCode()), safe(v.getDiagnosisText()),
                safe(v.getPrescription()), safe(v.getNotes()));
    }

    private String safe(String s) {
        if (s == null) return "";
        return s.replace("\\", "\\\\").replace("\"", "\\\"");
    }

    private void audit(long userId, String login, String action,
                       String entityType, String entityId, String details, String level) {
        try {
            auditLogDAO.log(new AuditLog(userId, login != null ? login : "system",
                    action, entityType, entityId, details, level));
        } catch (Exception ignored) {}
    }

    private Command.Builder ok(Command cmd) {
        return Command.response(cmd, CommandStatus.OK);
Продолжение приложения Б

    }

    private Command clientError(Command cmd, String msg) {
        return Command.response(cmd, CommandStatus.CLIENT_ERROR).message(msg).build();
    }

    private Command error(Command cmd, String msg) {
        return Command.response(cmd, CommandStatus.SERVER_ERROR).message(msg).build();
    }
}



Приложение В
(обязательное)
Листинг скрипта инициализации базы данных
     
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'role') THEN
        CREATE TYPE role AS ENUM ('PATIENT', 'DOCTOR', 'ADMIN');
    END IF;
END$$;

CREATE TABLE IF NOT EXISTS users (
    id BIGSERIAL PRIMARY KEY,
    login VARCHAR(255) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    salt VARCHAR(255),
    role role NOT NULL DEFAULT 'PATIENT',
    blocked BOOLEAN NOT NULL DEFAULT FALSE,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS sessions (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    token VARCHAR(255) NOT NULL UNIQUE,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP NOT NULL,
    ip_address VARCHAR(45)
);

CREATE TABLE IF NOT EXISTS specializations (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS doctors (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT REFERENCES users(id) ON DELETE SET NULL,
    full_name VARCHAR(150) NOT NULL,
    specialization_id INT REFERENCES specializations(id),
    office_number VARCHAR(10),
    education TEXT,
    about TEXT,
    experience_years INT DEFAULT 0,
    active BOOLEAN NOT NULL DEFAULT TRUE,
    price INT NOT NULL DEFAULT 0
);

CREATE TABLE IF NOT EXISTS patients (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    full_name VARCHAR(255) NOT NULL,
    phone VARCHAR(50),
    email VARCHAR(255),
    date_of_birth DATE,
    policy VARCHAR(100),
    allergy VARCHAR(500),
    blood_type VARCHAR(10)
);

CREATE TABLE IF NOT EXISTS schedule_slots (
    id BIGSERIAL PRIMARY KEY,
    doctor_id BIGINT NOT NULL REFERENCES doctors(id) ON DELETE CASCADE,
    slot_date DATE NOT NULL,
    start_time TIME NOT NULL,
    end_time TIME NOT NULL,
    booked BOOLEAN NOT NULL DEFAULT FALSE,
    blocked BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE TABLE IF NOT EXISTS appointments (
    id BIGSERIAL PRIMARY KEY,
    patient_id BIGINT NOT NULL REFERENCES patients(id) ON DELETE CASCADE,
    doctor_id BIGINT NOT NULL REFERENCES doctors(id) ON DELETE CASCADE,
    slot_id BIGINT REFERENCES schedule_slots(id) ON DELETE SET NULL,
    appointment_datetime TIMESTAMP NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'PENDING',
    reason TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS visit_details (
    id BIGSERIAL PRIMARY KEY,
    appointment_id BIGINT NOT NULL UNIQUE REFERENCES appointments(id) ON DELETE CASCADE,
    complaints TEXT,
    examination TEXT,
    diagnosis_code VARCHAR(50),
    diagnosis_text VARCHAR(500),
    prescription TEXT,
    notes TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS reminders (
    id BIGSERIAL PRIMARY KEY,
    appointment_id BIGINT NOT NULL REFERENCES appointments(id) ON DELETE CASCADE,
    scheduled_send_time TIMESTAMP NOT NULL,
    interval_minutes INT NOT NULL,
    channel VARCHAR(20) NOT NULL DEFAULT 'EMAIL',
    status VARCHAR(20) NOT NULL DEFAULT 'SCHEDULED',
    enabled BOOLEAN NOT NULL DEFAULT TRUE,
    actual_send_time TIMESTAMP
);

CREATE TABLE IF NOT EXISTS audit_log (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT,
    user_login VARCHAR(255),
    action VARCHAR(100),
    entity_type VARCHAR(100),
    entity_id VARCHAR(100),
    details TEXT,
    level VARCHAR(20),
    timestamp TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO specializations (name) VALUES
('Терапевт'), ('Стоматолог'), ('Хирург'), ('Невролог'),
('Офтальмолог'), ('Ортопед'), ('Эндокринолог'), ('Пульмонолог'),
('Дерматолог'), ('Кардиолог')
ON CONFLICT (name) DO NOTHING;

INSERT INTO doctors (full_name, specialization_id, office_number, education, experience_years, active, price) VALUES
('Смирнов Александр Владимирович', 1, '204', 'Первый МГМУ им. Сеченова', 18, true, 60),
('Иванова Мария Петровна',         2, '108', 'МГМСУ им. Евдокимова', 12, true, 250),
('Козлова Елена Викторовна',       1, '101', 'СПбГМУ им. Павлова', 20, true, 80),
('Петров Олег Николаевич',         3, '312', 'РНИМУ им. Пирогова', 15, true, 95),
('Белова Татьяна Юрьевна',         5, '220', 'Московский НИИ глазных болезней', 22, true, 120),
('Чернов Виктор Павлович',         6, '407', 'РМАПО', 16, true, 90),
('Фёдоров Игорь Сергеевич',        7, '305', 'НМИЦ эндокринологии', 14, true, 110),
('Орлова Надежда Алексеевна',      8, '213', 'РНИМУ им. Пирогова', 11, true, 95);

        2
     
